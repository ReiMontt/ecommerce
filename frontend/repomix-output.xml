This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
public/
  vite.svg
src/
  assets/
    react.svg
  components/
    storefront/
      MobileDrawer.tsx
      NavBar.tsx
      NavTop.tsx
      StorefrontNav.tsx
    ui/
      dropdown-menu.tsx
      separator.tsx
      sheet.tsx
    AddProductForm.tsx
    AuthGuard.tsx
    FeaturedScroll.tsx
    Footer.tsx
    Login.tsx
    LooksGallery.tsx
    Sidebar.tsx
  lib/
    api.ts
    utils.ts
  pages/
    CartPage.tsx
    LoginPage.tsx
    OrdersPage.tsx
    ProductsPage.tsx
    SuccessPage.tsx
  store/
    useCartStore.ts
  App.tsx
  index.css
  main.tsx
.gitignore
components.json
eslint.config.js
index.html
package.json
postcss.config.js
README.md
tailwind.config.js
tsconfig.app.json
tsconfig.json
tsconfig.node.json
vite.config.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="public/vite.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="iconify iconify--logos" width="31.88" height="32" preserveAspectRatio="xMidYMid meet" viewBox="0 0 256 257"><defs><linearGradient id="IconifyId1813088fe1fbc01fb466" x1="-.828%" x2="57.636%" y1="7.652%" y2="78.411%"><stop offset="0%" stop-color="#41D1FF"></stop><stop offset="100%" stop-color="#BD34FE"></stop></linearGradient><linearGradient id="IconifyId1813088fe1fbc01fb467" x1="43.376%" x2="50.316%" y1="2.242%" y2="89.03%"><stop offset="0%" stop-color="#FFEA83"></stop><stop offset="8.333%" stop-color="#FFDD35"></stop><stop offset="100%" stop-color="#FFA800"></stop></linearGradient></defs><path fill="url(#IconifyId1813088fe1fbc01fb466)" d="M255.153 37.938L134.897 252.976c-2.483 4.44-8.862 4.466-11.382.048L.875 37.958c-2.746-4.814 1.371-10.646 6.827-9.67l120.385 21.517a6.537 6.537 0 0 0 2.322-.004l117.867-21.483c5.438-.991 9.574 4.796 6.877 9.62Z"></path><path fill="url(#IconifyId1813088fe1fbc01fb467)" d="M185.432.063L96.44 17.501a3.268 3.268 0 0 0-2.634 3.014l-5.474 92.456a3.268 3.268 0 0 0 3.997 3.378l24.777-5.718c2.318-.535 4.413 1.507 3.936 3.838l-7.361 36.047c-.495 2.426 1.782 4.5 4.151 3.78l15.304-4.649c2.372-.72 4.652 1.36 4.15 3.788l-11.698 56.621c-.732 3.542 3.979 5.473 5.943 2.437l1.313-2.028l72.516-144.72c1.215-2.423-.88-5.186-3.54-4.672l-25.505 4.922c-2.396.462-4.435-1.77-3.759-4.114l16.646-57.705c.677-2.35-1.37-4.583-3.769-4.113Z"></path></svg>
</file>

<file path="src/assets/react.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="iconify iconify--logos" width="35.93" height="32" preserveAspectRatio="xMidYMid meet" viewBox="0 0 256 228"><path fill="#00D8FF" d="M210.483 73.824a171.49 171.49 0 0 0-8.24-2.597c.465-1.9.893-3.777 1.273-5.621c6.238-30.281 2.16-54.676-11.769-62.708c-13.355-7.7-35.196.329-57.254 19.526a171.23 171.23 0 0 0-6.375 5.848a155.866 155.866 0 0 0-4.241-3.917C100.759 3.829 77.587-4.822 63.673 3.233C50.33 10.957 46.379 33.89 51.995 62.588a170.974 170.974 0 0 0 1.892 8.48c-3.28.932-6.445 1.924-9.474 2.98C17.309 83.498 0 98.307 0 113.668c0 15.865 18.582 31.778 46.812 41.427a145.52 145.52 0 0 0 6.921 2.165a167.467 167.467 0 0 0-2.01 9.138c-5.354 28.2-1.173 50.591 12.134 58.266c13.744 7.926 36.812-.22 59.273-19.855a145.567 145.567 0 0 0 5.342-4.923a168.064 168.064 0 0 0 6.92 6.314c21.758 18.722 43.246 26.282 56.54 18.586c13.731-7.949 18.194-32.003 12.4-61.268a145.016 145.016 0 0 0-1.535-6.842c1.62-.48 3.21-.974 4.76-1.488c29.348-9.723 48.443-25.443 48.443-41.52c0-15.417-17.868-30.326-45.517-39.844Zm-6.365 70.984c-1.4.463-2.836.91-4.3 1.345c-3.24-10.257-7.612-21.163-12.963-32.432c5.106-11 9.31-21.767 12.459-31.957c2.619.758 5.16 1.557 7.61 2.4c23.69 8.156 38.14 20.213 38.14 29.504c0 9.896-15.606 22.743-40.946 31.14Zm-10.514 20.834c2.562 12.94 2.927 24.64 1.23 33.787c-1.524 8.219-4.59 13.698-8.382 15.893c-8.067 4.67-25.32-1.4-43.927-17.412a156.726 156.726 0 0 1-6.437-5.87c7.214-7.889 14.423-17.06 21.459-27.246c12.376-1.098 24.068-2.894 34.671-5.345a134.17 134.17 0 0 1 1.386 6.193ZM87.276 214.515c-7.882 2.783-14.16 2.863-17.955.675c-8.075-4.657-11.432-22.636-6.853-46.752a156.923 156.923 0 0 1 1.869-8.499c10.486 2.32 22.093 3.988 34.498 4.994c7.084 9.967 14.501 19.128 21.976 27.15a134.668 134.668 0 0 1-4.877 4.492c-9.933 8.682-19.886 14.842-28.658 17.94ZM50.35 144.747c-12.483-4.267-22.792-9.812-29.858-15.863c-6.35-5.437-9.555-10.836-9.555-15.216c0-9.322 13.897-21.212 37.076-29.293c2.813-.98 5.757-1.905 8.812-2.773c3.204 10.42 7.406 21.315 12.477 32.332c-5.137 11.18-9.399 22.249-12.634 32.792a134.718 134.718 0 0 1-6.318-1.979Zm12.378-84.26c-4.811-24.587-1.616-43.134 6.425-47.789c8.564-4.958 27.502 2.111 47.463 19.835a144.318 144.318 0 0 1 3.841 3.545c-7.438 7.987-14.787 17.08-21.808 26.988c-12.04 1.116-23.565 2.908-34.161 5.309a160.342 160.342 0 0 1-1.76-7.887Zm110.427 27.268a347.8 347.8 0 0 0-7.785-12.803c8.168 1.033 15.994 2.404 23.343 4.08c-2.206 7.072-4.956 14.465-8.193 22.045a381.151 381.151 0 0 0-7.365-13.322Zm-45.032-43.861c5.044 5.465 10.096 11.566 15.065 18.186a322.04 322.04 0 0 0-30.257-.006c4.974-6.559 10.069-12.652 15.192-18.18ZM82.802 87.83a323.167 323.167 0 0 0-7.227 13.238c-3.184-7.553-5.909-14.98-8.134-22.152c7.304-1.634 15.093-2.97 23.209-3.984a321.524 321.524 0 0 0-7.848 12.897Zm8.081 65.352c-8.385-.936-16.291-2.203-23.593-3.793c2.26-7.3 5.045-14.885 8.298-22.6a321.187 321.187 0 0 0 7.257 13.246c2.594 4.48 5.28 8.868 8.038 13.147Zm37.542 31.03c-5.184-5.592-10.354-11.779-15.403-18.433c4.902.192 9.899.29 14.978.29c5.218 0 10.376-.117 15.453-.343c-4.985 6.774-10.018 12.97-15.028 18.486Zm52.198-57.817c3.422 7.8 6.306 15.345 8.596 22.52c-7.422 1.694-15.436 3.058-23.88 4.071a382.417 382.417 0 0 0 7.859-13.026a347.403 347.403 0 0 0 7.425-13.565Zm-16.898 8.101a358.557 358.557 0 0 1-12.281 19.815a329.4 329.4 0 0 1-23.444.823c-7.967 0-15.716-.248-23.178-.732a310.202 310.202 0 0 1-12.513-19.846h.001a307.41 307.41 0 0 1-10.923-20.627a310.278 310.278 0 0 1 10.89-20.637l-.001.001a307.318 307.318 0 0 1 12.413-19.761c7.613-.576 15.42-.876 23.31-.876H128c7.926 0 15.743.303 23.354.883a329.357 329.357 0 0 1 12.335 19.695a358.489 358.489 0 0 1 11.036 20.54a329.472 329.472 0 0 1-11 20.722Zm22.56-122.124c8.572 4.944 11.906 24.881 6.52 51.026c-.344 1.668-.73 3.367-1.15 5.09c-10.622-2.452-22.155-4.275-34.23-5.408c-7.034-10.017-14.323-19.124-21.64-27.008a160.789 160.789 0 0 1 5.888-5.4c18.9-16.447 36.564-22.941 44.612-18.3ZM128 90.808c12.625 0 22.86 10.235 22.86 22.86s-10.235 22.86-22.86 22.86s-22.86-10.235-22.86-22.86s10.235-22.86 22.86-22.86Z"></path></svg>
</file>

<file path="src/components/storefront/MobileDrawer.tsx">
import { useEffect } from "react";
import { useNavigate } from "react-router-dom";
import {
  X,
  LayoutGrid,
  ClipboardList,
  ShoppingCart,
  LogOut,
  ChevronRight,
} from "lucide-react";
import { useCartStore } from "../../store/useCartStore";

const LIME = "#b8f53e";
const FF = "'Helvetica Neue', Arial, sans-serif";

interface MobileDrawerProps {
  open: boolean;
  onClose: () => void;
  categories: string[];
  activeCategory: string;
  onCategoryChange: (cat: string) => void;
}

export default function MobileDrawer({
  open,
  onClose,
  categories,
  activeCategory,
  onCategoryChange,
}: MobileDrawerProps) {
  const navigate = useNavigate();
  const totalItems = useCartStore((s) => s.totalItems());

  // Lock body scroll when open
  useEffect(() => {
    document.body.style.overflow = open ? "hidden" : "";
    return () => {
      document.body.style.overflow = "";
    };
  }, [open]);

  const go = (path: string) => {
    navigate(path);
    onClose();
  };
  const logout = () => {
    localStorage.removeItem("accessToken");
    navigate("/login");
    onClose();
  };

  return (
    <>
      {/* Backdrop */}
      <div
        onClick={onClose}
        style={{
          position: "fixed",
          inset: 0,
          background: "rgba(0,0,0,0.35)",
          zIndex: 998,
          opacity: open ? 1 : 0,
          pointerEvents: open ? "auto" : "none",
          transition: "opacity 0.25s ease",
          backdropFilter: "blur(4px)",
        }}
      />

      {/* Drawer panel */}
      <div
        style={{
          position: "fixed",
          top: 0,
          left: 0,
          height: "100vh",
          width: 300,
          background: "#fff",
          zIndex: 999,
          transform: open ? "translateX(0)" : "translateX(-100%)",
          transition: "transform 0.3s cubic-bezier(0.32, 0.72, 0, 1)",
          display: "flex",
          flexDirection: "column",
          fontFamily: FF,
          boxShadow: open ? "4px 0 40px rgba(0,0,0,0.12)" : "none",
          overflowY: "auto",
        }}
      >
        {/* Header */}
        <div
          style={{
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            padding: "20px 20px 16px",
            borderBottom: "1px solid #f0f0ec",
          }}
        >
          <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
              <path
                d="M12 2v20M2 12h20M4.93 4.93l14.14 14.14M19.07 4.93L4.93 19.07"
                stroke="#000"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
            <span
              style={{
                fontWeight: 900,
                fontSize: 16,
                color: "#000",
                letterSpacing: "-0.02em",
                fontStyle: "italic",
              }}
            >
              NexStore
            </span>
          </div>
          <button
            onClick={onClose}
            style={{
              background: "#f5f5f0",
              border: "none",
              cursor: "pointer",
              padding: 7,
              borderRadius: 8,
              display: "flex",
              alignItems: "center",
              color: "#555",
            }}
          >
            <X size={16} />
          </button>
        </div>

        {/* Nav links */}
        <div
          style={{
            padding: "12px 12px",
            display: "flex",
            flexDirection: "column",
            gap: 2,
          }}
        >
          <p
            style={{
              fontSize: 10,
              fontWeight: 900,
              color: "#bbb",
              textTransform: "uppercase",
              letterSpacing: "0.12em",
              padding: "4px 10px 8px",
            }}
          >
            Navigation
          </p>

          {[
            {
              label: "Marketplace",
              icon: <LayoutGrid size={16} />,
              action: () => go("/"),
            },
            {
              label: "Cart",
              icon: <ShoppingCart size={16} />,
              action: () => go("/cart"),
              badge: totalItems > 0 ? totalItems : null,
            },
            {
              label: "My Orders",
              icon: <ClipboardList size={16} />,
              action: () => go("/orders"),
            },
          ].map((item) => (
            <button
              key={item.label}
              onClick={item.action}
              style={{
                display: "flex",
                alignItems: "center",
                gap: 10,
                padding: "11px 12px",
                background: "none",
                border: "none",
                cursor: "pointer",
                borderRadius: 10,
                fontFamily: FF,
                fontSize: 14,
                fontWeight: 600,
                color: "#222",
                width: "100%",
                textAlign: "left",
                transition: "background 0.12s",
              }}
              onMouseEnter={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.background =
                  "#f5f5f0")
              }
              onMouseLeave={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.background =
                  "none")
              }
            >
              <span style={{ color: "#777" }}>{item.icon}</span>
              <span style={{ flex: 1 }}>{item.label}</span>
              {item.badge && (
                <span
                  style={{
                    background: LIME,
                    color: "#000",
                    fontSize: 10,
                    fontWeight: 900,
                    padding: "1px 7px",
                    borderRadius: 10,
                  }}
                >
                  {item.badge}
                </span>
              )}
              <ChevronRight size={14} color="#ccc" />
            </button>
          ))}
        </div>

        {/* Divider */}
        <div style={{ height: 1, background: "#f0f0ec", margin: "4px 20px" }} />

        {/* Categories */}
        <div style={{ padding: "12px 12px", flex: 1 }}>
          <p
            style={{
              fontSize: 10,
              fontWeight: 900,
              color: "#bbb",
              textTransform: "uppercase",
              letterSpacing: "0.12em",
              padding: "4px 10px 8px",
            }}
          >
            Categories
          </p>
          {categories.map((cat) => {
            const active = activeCategory === cat;
            return (
              <button
                key={cat}
                onClick={() => {
                  onCategoryChange(cat);
                  onClose();
                }}
                style={{
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "space-between",
                  width: "100%",
                  padding: "9px 12px",
                  background: active ? "#000" : "none",
                  border: "none",
                  cursor: "pointer",
                  borderRadius: 10,
                  fontFamily: FF,
                  fontSize: 13,
                  fontWeight: active ? 900 : 500,
                  color: active ? "#fff" : "#444",
                  textAlign: "left",
                  transition: "background 0.12s",
                }}
                onMouseEnter={(e) => {
                  if (!active)
                    (e.currentTarget as HTMLButtonElement).style.background =
                      "#f5f5f0";
                }}
                onMouseLeave={(e) => {
                  if (!active)
                    (e.currentTarget as HTMLButtonElement).style.background =
                      "none";
                }}
              >
                {cat}
                {active && (
                  <div
                    style={{
                      width: 6,
                      height: 6,
                      borderRadius: "50%",
                      background: LIME,
                    }}
                  />
                )}
              </button>
            );
          })}
        </div>

        {/* Logout */}
        <div style={{ padding: "12px 12px 28px" }}>
          <div
            style={{ height: 1, background: "#f0f0ec", margin: "0 0 12px" }}
          />
          <button
            onClick={logout}
            style={{
              display: "flex",
              alignItems: "center",
              gap: 8,
              padding: "10px 12px",
              background: "none",
              border: "none",
              cursor: "pointer",
              borderRadius: 10,
              fontFamily: FF,
              fontSize: 13,
              fontWeight: 600,
              color: "#999",
              width: "100%",
              transition: "color 0.15s, background 0.15s",
            }}
            onMouseEnter={(e) => {
              (e.currentTarget as HTMLButtonElement).style.color = "#e33";
              (e.currentTarget as HTMLButtonElement).style.background =
                "#fef2f2";
            }}
            onMouseLeave={(e) => {
              (e.currentTarget as HTMLButtonElement).style.color = "#999";
              (e.currentTarget as HTMLButtonElement).style.background = "none";
            }}
          >
            <LogOut size={15} />
            Sign Out
          </button>
        </div>
      </div>
    </>
  );
}
</file>

<file path="src/components/storefront/NavBar.tsx">
import { Search, ChevronDown, Sparkles } from "lucide-react";

export default function NavBar({
  search,
  onSearchChange,
  categories,
  activeCategory,
  onCategoryChange,
}: any) {
  const apiCats = categories.filter((c: string) => c !== "All");

  // Inside NavBar.tsx

  return (
    <div className="border-b border-slate-100">
      <div className="flex items-center gap-4 h-14 px-4 overflow-hidden">
        {/* Search Input - Expands */}
        <div className="relative flex-1 group">
          <Search
            size={14}
            className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400"
          />
          <input
            type="text"
            placeholder="Search..."
            className="w-full pl-10 pr-4 py-2 bg-slate-100 rounded-full text-sm outline-none focus:bg-white border-2 border-transparent focus:border-[#b8f53e]/50"
          />
        </div>

        {/* Category List - Horizontal Scroll on Mobile */}
        <div className="flex items-center gap-2 overflow-x-auto no-scrollbar scroll-smooth flex-nowrap">
          {categories.map((cat) => (
            <button
              key={cat}
              onClick={() => onCategoryChange(cat)}
              className={`px-4 py-2 rounded-full text-[10px] font-black uppercase tracking-widest whitespace-nowrap transition-all ${
                activeCategory === cat
                  ? "bg-black text-white"
                  : "bg-white border border-slate-200 text-slate-500 hover:border-black hover:text-black"
              }`}
            >
              {cat}
            </button>
          ))}
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/components/storefront/NavTop.tsx">
import { useNavigate } from "react-router-dom";
import { ShoppingCart, Menu, HelpCircle, Info } from "lucide-react";
import { useCartStore } from "../../store/useCartStore";

export default function NavTop({ onMenuToggle }: { onMenuToggle: () => void }) {
  const navigate = useNavigate();
  const totalItems = useCartStore((s) => s.totalItems());

  return (
    <div className="flex items-center justify-between h-14 px-4 md:px-8 border-b border-slate-50">
      {/* LEFT: Hamburger */}
      <div className="flex-1 flex items-center">
        <button
          onClick={onMenuToggle}
          className="p-2 hover:bg-slate-100 rounded-xl transition-colors text-slate-600"
        >
          <Menu size={20} />
        </button>
      </div>

      {/* CENTER: Logo (Properly centered in the middle of the screen) */}
      <button
        onClick={() => navigate("/")}
        className="flex items-center gap-2 group transition-transform active:scale-95"
      >
        <svg
          width="22"
          height="22"
          viewBox="0 0 24 24"
          fill="none"
          className="text-blue-600 group-hover:rotate-90 transition-transform duration-500"
        >
          <path
            d="M12 2v20M2 12h20M4.93 4.93l14.14 14.14M19.07 4.93L4.93 19.07"
            stroke="currentColor"
            strokeWidth="2.5"
            strokeLinecap="round"
          />
        </svg>
        <span className="font-black text-xl tracking-tighter italic text-slate-900">
          NexStore
        </span>
      </button>

      {/* RIGHT: Actions */}
      <div className="flex-1 flex items-center justify-end gap-1 md:gap-3">
        <button className="hidden sm:flex items-center gap-1.5 px-3 py-1.5 text-sm font-bold text-slate-500 hover:text-blue-600 transition-colors">
          <Info size={16} /> About
        </button>
        <button className="hidden sm:flex items-center gap-1.5 px-3 py-1.5 text-sm font-bold text-slate-500 hover:text-blue-600 transition-colors">
          <HelpCircle size={16} /> FAQs
        </button>

        <div className="w-px h-6 bg-slate-100 mx-1 hidden sm:block" />

        {/* Cart: Fixed badge centering */}
        <button
          onClick={() => navigate("/cart")}
          className="relative p-2.5 hover:bg-slate-100 rounded-xl transition-all text-slate-700 active:scale-90"
        >
          <ShoppingCart size={22} strokeWidth={2.5} />
          {totalItems > 0 && (
            <span className="absolute top-1 right-1 bg-[#b8f53e] text-black text-[10px] font-black h-5 w-5 flex items-center justify-center rounded-full border-2 border-white leading-none shadow-sm">
              {totalItems > 9 ? "9+" : totalItems}
            </span>
          )}
        </button>
      </div>
    </div>
  );
}
</file>

<file path="src/components/storefront/StorefrontNav.tsx">
import { useState, useEffect } from "react";
import {
  LogOut,
  ShoppingBag,
  LayoutGrid,
  Receipt,
  ChevronRight,
  Menu,
  Search,
  ShoppingCart,
  ChevronDown,
} from "lucide-react";
import { useNavigate, Link } from "react-router-dom";
import { useCartStore } from "../../store/useCartStore";
import {
  Sheet,
  SheetContent,
  SheetTrigger,
  SheetHeader,
  SheetTitle,
} from "@/components/ui/sheet";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";

/* ── SUB-COMPONENT: Sidebar User Profile ── */
function SidebarProfile({ email }: { email: string | null }) {
  const initial = email ? email.charAt(0).toUpperCase() : "G";
  const name = email ? email.split("@")[0].toUpperCase() : "GUEST USER";

  return (
    <div className="flex items-center gap-4 p-5 bg-white/5 rounded-[2rem] border border-white/10 mb-12">
      <div className="w-12 h-12 rounded-full bg-[#b8f53e] flex items-center justify-center text-black font-black text-xl flex-shrink-0">
        {initial}
      </div>
      <div className="flex flex-col min-w-0">
        <span className="text-white font-black uppercase tracking-tighter font-compressed text-xl leading-none truncate">
          {name}
        </span>
        <span className="text-slate-500 text-[10px] font-bold uppercase tracking-[0.2em] mt-1.5 truncate">
          {email || "MEMBER ACCESS"}
        </span>
      </div>
    </div>
  );
}

/* ── SUB-COMPONENT: Sidebar Links ── */
function MobileNavLink({
  to,
  label,
  icon,
}: {
  to: string;
  label: string;
  icon: React.ReactNode;
}) {
  return (
    <Link
      to={to}
      className="flex items-center justify-between p-4 rounded-2xl hover:bg-white/5 transition-all group border border-transparent hover:border-white/5"
    >
      <div className="flex items-center gap-4 text-white/90 font-black uppercase font-compressed tracking-tight text-xl group-hover:text-[#b8f53e] transition-colors">
        <span className="text-slate-500 group-hover:text-[#b8f53e]">
          {icon}
        </span>
        {label}
      </div>
      <ChevronRight
        size={18}
        className="text-slate-800 group-hover:text-[#b8f53e] group-hover:translate-x-1 transition-all"
      />
    </Link>
  );
}

/* ── MAIN COMPONENT ── */
export default function StorefrontNav({
  search = "", // Default value to prevent undefined
  onSearchChange = () => {}, // No-op fallback
  categories = [], // Default empty array
  activeCategory = "ALL", // Default string
  onCategoryChange = () => {},
  onLogout,
}: any) {
  const navigate = useNavigate();
  const totalItems = useCartStore((s) => s.totalItems());
  const userEmail = localStorage.getItem("userEmail");

  return (
    <header className="sticky top-0 z-[100] w-full bg-white/95 backdrop-blur-md border-b border-slate-100">
      <div className="max-w-7xl mx-auto px-5 md:px-10">
        <div className="flex items-center justify-between h-16 relative">
          <div className="flex-1 flex items-center">
            <Sheet>
              <SheetTrigger asChild>
                <button className="p-2 -ml-2 hover:bg-slate-100 rounded-full transition-colors active:scale-90">
                  <Menu size={22} strokeWidth={2.5} />
                </button>
              </SheetTrigger>
              <SheetContent
                side="left"
                className="w-[340px] border-none bg-black text-white p-0 overflow-hidden flex flex-col rounded-r-[2.5rem] z-[1001]"
              >
                <div className="flex flex-col h-full p-8 pt-10">
                  <header className="mb-10">
                    <p className="text-[#b8f53e] font-black text-[10px] uppercase tracking-[0.4em] mb-2">
                      NexStore Platform
                    </p>
                    <h2 className="text-white font-black text-xs uppercase tracking-[0.2em] opacity-40">
                      User Dashboard v1.0.4
                    </h2>
                  </header>

                  <SidebarProfile email={userEmail} />

                  <nav className="flex flex-col gap-1">
                    <p className="text-slate-600 font-black text-[10px] uppercase tracking-[0.3em] mb-4 px-4">
                      Navigation
                    </p>
                    <MobileNavLink
                      to="/"
                      icon={<LayoutGrid size={20} />}
                      label="Marketplace"
                    />
                    <MobileNavLink
                      to="/orders"
                      icon={<Receipt size={20} />}
                      label="My Orders"
                    />
                    <MobileNavLink
                      to="/cart"
                      icon={<ShoppingBag size={20} />}
                      label="Checkout"
                    />
                  </nav>

                  <div className="mt-auto">
                    <button
                      onClick={onLogout}
                      className="flex items-center justify-between w-full p-5 rounded-[1.5rem] bg-red-500/10 text-red-500 hover:bg-red-500 hover:text-white transition-all group active:scale-95 border border-red-500/20"
                    >
                      <div className="flex items-center gap-3 font-black uppercase font-compressed text-xl tracking-tight">
                        <LogOut size={20} />
                        Logout
                      </div>
                      <ChevronRight
                        size={16}
                        className="group-hover:translate-x-1 transition-transform"
                      />
                    </button>
                  </div>
                </div>
              </SheetContent>
            </Sheet>
          </div>

          <button
            onClick={() => navigate("/")}
            className="absolute left-1/2 -translate-x-1/2 z-20"
          >
            <span className="font-black text-2xl md:text-3xl italic tracking-tighter text-black uppercase font-compressed">
              NexStore
            </span>
          </button>

          <div className="flex-1 flex items-center justify-end">
            <button
              onClick={() => navigate("/cart")}
              className="relative p-2 hover:bg-slate-100 rounded-xl transition-all active:scale-90"
            >
              <ShoppingCart size={22} strokeWidth={2.3} />
              {totalItems > 0 && (
                <span className="absolute top-0 right-0 bg-[#b8f53e] text-black text-[10px] font-black h-5 w-5 flex items-center justify-center rounded-full border-2 border-white leading-none shadow-sm translate-x-1 -translate-y-1">
                  {totalItems}
                </span>
              )}
            </button>
          </div>
        </div>

        {/* ROW 2 - Added Optional Chaining to prevent .toUpperCase() error */}
        <div className="flex items-center gap-3 h-14 pb-2 md:pb-0">
          <DropdownMenu>
            <DropdownMenuTrigger className="flex items-center gap-1.5 px-3 py-2 bg-slate-50 border border-slate-100 rounded-full text-[10px] font-black uppercase tracking-widest outline-none hover:bg-slate-100 transition-colors">
              {/* FIX: Check for undefined and "string" keyword */}
              {(activeCategory || "ALL") === "string"
                ? "ALL"
                : activeCategory?.toUpperCase() || "ALL"}{" "}
              <ChevronDown size={12} />
            </DropdownMenuTrigger>
            <DropdownMenuContent
              align="start"
              className="rounded-2xl p-2 w-48 shadow-2xl z-[110]"
            >
              {/* FIX: Ensure categories exists before mapping */}
              {categories?.map((cat: string) => (
                <DropdownMenuItem
                  key={cat}
                  onClick={() => onCategoryChange?.(cat)}
                  className="rounded-xl font-bold text-xs uppercase p-3 cursor-pointer hover:bg-[#b8f53e] hover:text-black transition-colors"
                >
                  {cat === "string" ? "All" : cat}
                </DropdownMenuItem>
              )) || <DropdownMenuItem>No Categories</DropdownMenuItem>}
            </DropdownMenuContent>
          </DropdownMenu>

          <div className="relative flex-1 group min-w-0">
            <Search
              size={14}
              className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 group-focus-within:text-black transition-colors"
            />
            <input
              type="text"
              placeholder="SEARCH COLLECTION..."
              value={search || ""} // FIX: Fallback to empty string
              onChange={(e) => onSearchChange?.(e.target.value)}
              className="w-full pl-11 pr-4 py-2.5 bg-slate-50 border-none rounded-full text-[10px] font-bold tracking-widest outline-none focus:bg-white focus:ring-1 focus:ring-slate-200 transition-all"
            />
          </div>
        </div>
      </div>
    </header>
  );
}
</file>

<file path="src/components/ui/dropdown-menu.tsx">
import * as React from "react";
import * as DropdownMenuPrimitive from "@radix-ui/react-dropdown-menu";
import { Check, ChevronRight, Circle } from "lucide-react";

import { cn } from "../../lib/utils";

const DropdownMenu = DropdownMenuPrimitive.Root;

const DropdownMenuTrigger = DropdownMenuPrimitive.Trigger;

const DropdownMenuGroup = DropdownMenuPrimitive.Group;

const DropdownMenuPortal = DropdownMenuPrimitive.Portal;

const DropdownMenuSub = DropdownMenuPrimitive.Sub;

const DropdownMenuRadioGroup = DropdownMenuPrimitive.RadioGroup;

const DropdownMenuSubTrigger = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.SubTrigger>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.SubTrigger> & {
    inset?: boolean;
  }
>(({ className, inset, children, ...props }, ref) => (
  <DropdownMenuPrimitive.SubTrigger
    ref={ref}
    className={cn(
      "flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-sm outline-none focus:bg-accent data-[state=open]:bg-accent [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0",
      inset && "pl-8",
      className,
    )}
    {...props}
  >
    {children}
    <ChevronRight className="ml-auto" />
  </DropdownMenuPrimitive.SubTrigger>
));
DropdownMenuSubTrigger.displayName =
  DropdownMenuPrimitive.SubTrigger.displayName;

const DropdownMenuSubContent = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.SubContent>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.SubContent>
>(({ className, ...props }, ref) => (
  <DropdownMenuPrimitive.SubContent
    ref={ref}
    className={cn(
      "z-50 min-w-[8rem] overflow-hidden rounded-md border bg-popover p-1 text-popover-foreground shadow-lg data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 origin-[--radix-dropdown-menu-content-transform-origin]",
      className,
    )}
    {...props}
  />
));
DropdownMenuSubContent.displayName =
  DropdownMenuPrimitive.SubContent.displayName;

const DropdownMenuContent = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Content>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <DropdownMenuPrimitive.Portal>
    <DropdownMenuPrimitive.Content
      ref={ref}
      sideOffset={sideOffset}
      className={cn(
        "z-50 max-h-[var(--radix-dropdown-menu-content-available-height)] min-w-[8rem] overflow-y-auto overflow-x-hidden rounded-md border bg-popover p-1 text-popover-foreground shadow-md",
        "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 origin-[--radix-dropdown-menu-content-transform-origin]",
        className,
      )}
      {...props}
    />
  </DropdownMenuPrimitive.Portal>
));
DropdownMenuContent.displayName = DropdownMenuPrimitive.Content.displayName;

const DropdownMenuItem = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Item> & {
    inset?: boolean;
  }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Item
    ref={ref}
    className={cn(
      "relative flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-sm outline-none transition-colors focus:bg-accent focus:text-accent-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50 [&>svg]:size-4 [&>svg]:shrink-0",
      inset && "pl-8",
      className,
    )}
    {...props}
  />
));
DropdownMenuItem.displayName = DropdownMenuPrimitive.Item.displayName;

const DropdownMenuCheckboxItem = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.CheckboxItem>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.CheckboxItem>
>(({ className, children, checked, ...props }, ref) => (
  <DropdownMenuPrimitive.CheckboxItem
    ref={ref}
    className={cn(
      "relative flex cursor-default select-none items-center rounded-sm py-1.5 pl-8 pr-2 text-sm outline-none transition-colors focus:bg-accent focus:text-accent-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
      className,
    )}
    checked={checked}
    {...props}
  >
    <span className="absolute left-2 flex h-3.5 w-3.5 items-center justify-center">
      <DropdownMenuPrimitive.ItemIndicator>
        <Check className="h-4 w-4" />
      </DropdownMenuPrimitive.ItemIndicator>
    </span>
    {children}
  </DropdownMenuPrimitive.CheckboxItem>
));
DropdownMenuCheckboxItem.displayName =
  DropdownMenuPrimitive.CheckboxItem.displayName;

const DropdownMenuRadioItem = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.RadioItem>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.RadioItem>
>(({ className, children, ...props }, ref) => (
  <DropdownMenuPrimitive.RadioItem
    ref={ref}
    className={cn(
      "relative flex cursor-default select-none items-center rounded-sm py-1.5 pl-8 pr-2 text-sm outline-none transition-colors focus:bg-accent focus:text-accent-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
      className,
    )}
    {...props}
  >
    <span className="absolute left-2 flex h-3.5 w-3.5 items-center justify-center">
      <DropdownMenuPrimitive.ItemIndicator>
        <Circle className="h-2 w-2 fill-current" />
      </DropdownMenuPrimitive.ItemIndicator>
    </span>
    {children}
  </DropdownMenuPrimitive.RadioItem>
));
DropdownMenuRadioItem.displayName = DropdownMenuPrimitive.RadioItem.displayName;

const DropdownMenuLabel = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Label>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Label> & {
    inset?: boolean;
  }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Label
    ref={ref}
    className={cn(
      "px-2 py-1.5 text-sm font-semibold",
      inset && "pl-8",
      className,
    )}
    {...props}
  />
));
DropdownMenuLabel.displayName = DropdownMenuPrimitive.Label.displayName;

const DropdownMenuSeparator = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Separator>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Separator>
>(({ className, ...props }, ref) => (
  <DropdownMenuPrimitive.Separator
    ref={ref}
    className={cn("-mx-1 my-1 h-px bg-muted", className)}
    {...props}
  />
));
DropdownMenuSeparator.displayName = DropdownMenuPrimitive.Separator.displayName;

const DropdownMenuShortcut = ({
  className,
  ...props
}: React.HTMLAttributes<HTMLSpanElement>) => {
  return (
    <span
      className={cn("ml-auto text-xs tracking-widest opacity-60", className)}
      {...props}
    />
  );
};
DropdownMenuShortcut.displayName = "DropdownMenuShortcut";

export {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuCheckboxItem,
  DropdownMenuRadioItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuShortcut,
  DropdownMenuGroup,
  DropdownMenuPortal,
  DropdownMenuSub,
  DropdownMenuSubContent,
  DropdownMenuSubTrigger,
  DropdownMenuRadioGroup,
};
</file>

<file path="src/components/ui/separator.tsx">
import * as React from "react";
import * as SeparatorPrimitive from "@radix-ui/react-separator";

import { cn } from "../../lib/utils";

const Separator = React.forwardRef<
  React.ElementRef<typeof SeparatorPrimitive.Root>,
  React.ComponentPropsWithoutRef<typeof SeparatorPrimitive.Root>
>(
  (
    { className, orientation = "horizontal", decorative = true, ...props },
    ref,
  ) => (
    <SeparatorPrimitive.Root
      ref={ref}
      decorative={decorative}
      orientation={orientation}
      className={cn(
        "shrink-0 bg-border",
        orientation === "horizontal" ? "h-[1px] w-full" : "h-full w-[1px]",
        className,
      )}
      {...props}
    />
  ),
);
Separator.displayName = SeparatorPrimitive.Root.displayName;

export { Separator };
</file>

<file path="src/components/ui/sheet.tsx">
import * as React from "react";
import * as SheetPrimitive from "@radix-ui/react-dialog";
import { cva, type VariantProps } from "class-variance-authority";
import { X } from "lucide-react";

import { cn } from "@/lib/utils";

const Sheet = SheetPrimitive.Root;

const SheetTrigger = SheetPrimitive.Trigger;

const SheetClose = SheetPrimitive.Close;

const SheetPortal = SheetPrimitive.Portal;

const SheetOverlay = React.forwardRef<
  React.ElementRef<typeof SheetPrimitive.Overlay>,
  React.ComponentPropsWithoutRef<typeof SheetPrimitive.Overlay>
>(({ className, ...props }, ref) => (
  <SheetPrimitive.Overlay
    className={cn(
      "fixed inset-0 z-50 bg-black/80  data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
      className,
    )}
    {...props}
    ref={ref}
  />
));
SheetOverlay.displayName = SheetPrimitive.Overlay.displayName;

const sheetVariants = cva(
  "fixed z-50 gap-4 bg-background p-6 shadow-lg transition ease-in-out data-[state=closed]:duration-300 data-[state=open]:duration-500 data-[state=open]:animate-in data-[state=closed]:animate-out",
  {
    variants: {
      side: {
        top: "inset-x-0 top-0 border-b data-[state=closed]:slide-out-to-top data-[state=open]:slide-in-from-top",
        bottom:
          "inset-x-0 bottom-0 border-t data-[state=closed]:slide-out-to-bottom data-[state=open]:slide-in-from-bottom",
        left: "inset-y-0 left-0 h-full w-3/4 border-r data-[state=closed]:slide-out-to-left data-[state=open]:slide-in-from-left sm:max-w-sm",
        right:
          "inset-y-0 right-0 h-full w-3/4 border-l data-[state=closed]:slide-out-to-right data-[state=open]:slide-in-from-right sm:max-w-sm",
      },
    },
    defaultVariants: {
      side: "right",
    },
  },
);

interface SheetContentProps
  extends
    React.ComponentPropsWithoutRef<typeof SheetPrimitive.Content>,
    VariantProps<typeof sheetVariants> {}

const SheetContent = React.forwardRef<
  React.ElementRef<typeof SheetPrimitive.Content>,
  SheetContentProps
>(({ side = "right", className, children, ...props }, ref) => (
  <SheetPortal>
    <SheetOverlay />
    <SheetPrimitive.Content
      ref={ref}
      className={cn(sheetVariants({ side }), className)}
      {...props}
    >
      <SheetPrimitive.Close className="absolute right-6 top-6 rounded-full opacity-70 ring-offset-background transition-opacity hover:opacity-100 hover:bg-white/10 p-2 outline-none focus:ring-2 focus:ring-[#b8f53e] disabled:pointer-events-none data-[state=open]:bg-secondary">
        <X className="h-5 w-5 text-white" />
        <span className="sr-only">Close</span>
      </SheetPrimitive.Close>
      {children}
    </SheetPrimitive.Content>
  </SheetPortal>
));
SheetContent.displayName = SheetPrimitive.Content.displayName;

const SheetHeader = ({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) => (
  <div
    className={cn(
      "flex flex-col space-y-2 text-center sm:text-left",
      className,
    )}
    {...props}
  />
);
SheetHeader.displayName = "SheetHeader";

const SheetFooter = ({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) => (
  <div
    className={cn(
      "flex flex-col-reverse sm:flex-row sm:justify-end sm:space-x-2",
      className,
    )}
    {...props}
  />
);
SheetFooter.displayName = "SheetFooter";

const SheetTitle = React.forwardRef<
  React.ElementRef<typeof SheetPrimitive.Title>,
  React.ComponentPropsWithoutRef<typeof SheetPrimitive.Title>
>(({ className, ...props }, ref) => (
  <SheetPrimitive.Title
    ref={ref}
    className={cn("text-lg font-semibold text-foreground", className)}
    {...props}
  />
));
SheetTitle.displayName = SheetPrimitive.Title.displayName;

const SheetDescription = React.forwardRef<
  React.ElementRef<typeof SheetPrimitive.Description>,
  React.ComponentPropsWithoutRef<typeof SheetPrimitive.Description>
>(({ className, ...props }, ref) => (
  <SheetPrimitive.Description
    ref={ref}
    className={cn("text-sm text-muted-foreground", className)}
    {...props}
  />
));
SheetDescription.displayName = SheetPrimitive.Description.displayName;

export {
  Sheet,
  SheetPortal,
  SheetOverlay,
  SheetTrigger,
  SheetClose,
  SheetContent,
  SheetHeader,
  SheetFooter,
  SheetTitle,
  SheetDescription,
};
</file>

<file path="src/components/AddProductForm.tsx">
import { useState } from "react";
import { api } from "../lib/api";
import { useQueryClient } from "@tanstack/react-query";

export function AddProductForm({ close }: { close: () => void }) {
  const queryClient = useQueryClient();
  const [form, setForm] = useState({
    name: "",
    price: 0,
    category: "Electronics",
    description: "",
    imageUrl: "",
  });
  const [errors, setErrors] = useState<string[]>([]); // To show errors to user

  const save = async () => {
    setErrors([]);

    try {
      await api.post("/product", form);
      queryClient.invalidateQueries({ queryKey: ["product"] });
      close();
    } catch (err: any) {
      // 1. Log the error so you can see the structure in the console
      console.log("Full Error Object:", err.response?.data);

      if (err.response?.status === 400) {
        // 2. ASP.NET validation errors are inside 'errors'
        const validationData = err.response.data.errors;

        if (validationData) {
          // 3. This converts { Name: ["Error1"], Price: ["Error2"] }
          // into ["Error1", "Error2"]
          const messages = Object.values(validationData).flat() as string[];
          setErrors(messages);
        } else {
          // Fallback for non-validation 400 errors
          setErrors(["Invalid request sent to server."]);
        }
      } else {
        setErrors(["Server error. Check if backend is running."]);
      }
    }
  };

  return (
    <div className="fixed inset-0 bg-black/50 flex items-center justify-center p-4 z-50">
      <div className="bg-white p-6 rounded-2xl w-full max-w-md shadow-2xl">
        <h2 className="text-xl font-bold mb-4">Add New Product</h2>

        {/* Display Validation Errors */}
        {errors.length > 0 && (
          <div className="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg">
            {errors.map((msg, i) => (
              <p key={i} className="text-red-600 text-sm">
                • {msg}
              </p>
            ))}
          </div>
        )}

        <input
          placeholder="Name"
          className="w-full p-2 border rounded mb-2 focus:ring-2 focus:ring-blue-500 outline-none"
          onChange={(e) => setForm({ ...form, name: e.target.value })}
        />
        <input
          placeholder="Price"
          type="number"
          className="w-full p-2 border rounded mb-4 focus:ring-2 focus:ring-blue-500 outline-none"
          onChange={(e) => setForm({ ...form, price: Number(e.target.value) })}
        />

        <div className="flex gap-2">
          <button
            onClick={save}
            className="flex-1 bg-blue-600 text-white p-2 rounded-xl font-bold hover:bg-blue-700 transition"
          >
            Save Product
          </button>
          <button
            onClick={close}
            className="px-4 text-gray-500 hover:text-gray-700"
          >
            Cancel
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/components/AuthGuard.tsx">
import { Navigate, Outlet } from "react-router-dom";

// 🛡️ PROTECTED ROUTE: Only logged in users can enter
export const ProtectedRoute = () => {
  const token = localStorage.getItem("token");
  // If no token, redirect to login
  if (!token) return <Navigate to="/login" replace />;
  // If token exists, render the child routes (Marketplace, Cart, etc)
  return <Outlet />;
};

// 🔓 PUBLIC ROUTE: Logged in users cannot go back to Login/Register
export const PublicRoute = () => {
  const token = localStorage.getItem("token");
  if (token) return <Navigate to="/" replace />;
  return <Outlet />;
};
</file>

<file path="src/components/FeaturedScroll.tsx">
import { useRef, useState } from "react";
import { ArrowRight, ChevronRight } from "lucide-react";

const SCROLL_ITEMS = [
  {
    id: 1,
    label: "JACKET",
    image:
      "https://images.unsplash.com/photo-1591047139829-d91aecb6caea?w=800&q=80",
  },
  {
    id: 2,
    label: "DENIM",
    image:
      "https://images.unsplash.com/photo-1541099649105-f69ad21f3246?w=800&q=80",
  },
  {
    id: 3,
    label: "OUTERWEAR",
    image:
      "https://images.unsplash.com/photo-1539533318441-6063079b7631?w=800&q=80",
  },
  {
    id: 4,
    label: "ARCHIVE",
    image:
      "https://images.unsplash.com/photo-1551488831-00ddcb6c6bd3?w=800&q=80",
  },
];

export default function FeaturedScroll() {
  const scrollRef = useRef<HTMLDivElement>(null);
  const [showGuide, setShowGuide] = useState(true);

  const handleScroll = () => {
    if (!scrollRef.current) return;
    const s = scrollRef.current.scrollLeft;
    // Only toggle the guide arrow
    if (s > 100) setShowGuide(false);
    else setShowGuide(true);
  };

  return (
    /* ── OUTER WRAPPER: This makes the section full-bleed black ── */
    <section className="w-full bg-black py-20 md:py-32 overflow-hidden relative">
      {/* ── INNER CONTAINER: This aligns the text with your site margins ── */}
      <div className="max-w-7xl mx-auto px-5 md:px-10">
        <div className="flex flex-col md:flex-row h-auto md:h-[650px] w-full relative gap-8 md:gap-0">
          {/* FIXED LEFT BLOCK: No longer fades out */}
          <div
            className="md:sticky md:top-0 z-30 flex-shrink-0 w-full md:w-[400px] bg-black flex flex-col justify-center py-6 md:pr-16"
            style={{
              backgroundImage: `linear-gradient(45deg, #0a0a0a 25%, transparent 25%, transparent 50%, #0a0a0a 50%, #0a0a0a 75%, transparent 75%, transparent)`,
              backgroundSize: "4px 4px",
            }}
          >
            <h2 className="text-white text-4xl md:text-7xl font-black uppercase font-compressed leading-[0.8] tracking-tighter italic">
              New <br /> Styles <br /> For A <br /> Modern <br /> Look
            </h2>
            <div className="mt-8 md:mt-12 flex items-center gap-2 text-[#b8f53e] font-black text-[10px] tracking-[0.3em] uppercase">
              Featured <ArrowRight size={14} />
            </div>
          </div>

          {/* HORIZONTAL SCROLL AREA */}
          <div
            ref={scrollRef}
            onScroll={handleScroll}
            className="flex overflow-x-auto no-scrollbar snap-x snap-mandatory w-full bg-black"
          >
            {SCROLL_ITEMS.map((item) => (
              <div
                key={item.id}
                className="relative flex-shrink-0 w-[85vw] md:w-[500px] h-[400px] md:h-full snap-start border-r border-white/5"
              >
                <img
                  src={item.image}
                  className="w-full h-full object-cover"
                  alt={item.label}
                />
                <div className="absolute bottom-10 left-10 z-10">
                  <span className="text-white font-black text-[11px] tracking-[0.5em] uppercase px-4 py-2 bg-black/40 backdrop-blur-md">
                    {item.label}
                  </span>
                </div>
              </div>
            ))}
            {/* End spacer for better scrolling feel */}
            <div className="flex-shrink-0 w-[10vw] md:w-[20vw]" />
          </div>

          {/* FLOATING GUIDE ARROW (Only visible on desktop) */}
          <div
            className={`absolute right-0 top-1/2 -translate-y-1/2 z-40 pointer-events-none transition-all duration-700 hidden md:block ${showGuide ? "opacity-100" : "opacity-0"}`}
          >
            <div className="flex flex-col items-center animate-bounce">
              <div className="p-4 rounded-full bg-white/10 backdrop-blur-md border border-white/20">
                <ChevronRight size={32} className="text-[#b8f53e]" />
              </div>
              <span className="mt-4 text-[#b8f53e] font-black text-[9px] uppercase tracking-[0.6em] [writing-mode:vertical-lr]">
                Scroll
              </span>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
</file>

<file path="src/components/Footer.tsx">
import { Github, Twitter, Instagram, Facebook } from "lucide-react";
import { Link } from "react-router-dom";

const footerSections = [
  {
    title: "Collection",
    links: [
      { title: "All Products", href: "/" },
      { title: "New Arrivals", href: "#" },
      { title: "Best Sellers", href: "#" },
    ],
  },
  {
    title: "Support",
    links: [
      { title: "Shipping", href: "#" },
      { title: "Returns", href: "#" },
      { title: "Contact", href: "#" },
    ],
  },
  {
    title: "Legal",
    links: [
      { title: "Privacy", href: "#" },
      { title: "Terms", href: "#" },
    ],
  },
];

const Footer = () => {
  return (
    <footer className="bg-black text-white pt-20 pb-10">
      <div className="max-w-7xl mx-auto px-6 sm:px-10">
        {/* Main Footer Content */}
        <div className="flex flex-col lg:flex-row lg:justify-between gap-16">
          {/* LEFT: Brand Info */}
          <div className="max-w-sm">
            <span className="font-black text-3xl italic tracking-tighter uppercase font-compressed block mb-6">
              NexStore
            </span>
            <p className="text-slate-400 text-[11px] font-bold uppercase tracking-[0.15em] leading-relaxed mb-8">
              Redefining tech aesthetics for the modern era. Designed in Manila,
              built for the digital vanguard.
            </p>
            <div className="flex items-center gap-6 text-slate-500">
              <a href="#" className="hover:text-[#b8f53e] transition-colors">
                <Twitter size={18} />
              </a>
              <a href="#" className="hover:text-[#b8f53e] transition-colors">
                <Instagram size={18} />
              </a>
              <a href="#" className="hover:text-[#b8f53e] transition-colors">
                <Facebook size={18} />
              </a>
              <a href="#" className="hover:text-[#b8f53e] transition-colors">
                <Github size={18} />
              </a>
            </div>
          </div>

          {/* RIGHT: Links (Stacked on mobile, spread on desktop) */}
          <div className="grid grid-cols-2 md:grid-cols-3 gap-12 lg:gap-24">
            {footerSections.map((section) => (
              <div key={section.title}>
                <h6 className="text-[#b8f53e] font-black uppercase text-[10px] tracking-[0.2em] mb-6">
                  {section.title}
                </h6>
                <ul className="space-y-4">
                  {section.links.map((link) => (
                    <li key={link.title}>
                      <a
                        href={link.href}
                        className="text-slate-400 hover:text-white text-[11px] font-bold uppercase tracking-widest transition-colors"
                      >
                        {link.title}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>

        {/* BOTTOM: Legal */}
        <div className="mt-20 pt-10 border-t border-white/5 flex flex-col md:flex-row justify-between items-center gap-6">
          <p className="text-slate-600 font-black text-[9px] uppercase tracking-[0.4em] text-center md:text-left">
            &copy; {new Date().getFullYear()} NEXSTORE INTERNATIONAL. ALL RIGHTS
            RESERVED.
          </p>
          <div className="flex items-center gap-4 text-slate-600 font-black text-[9px] uppercase tracking-[0.3em]">
            <span>Manila</span>
            <div className="w-1 h-1 bg-white/10 rounded-full" />
            <span>Sydney</span>
            <div className="w-1 h-1 bg-white/10 rounded-full" />
            <span>London</span>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
</file>

<file path="src/components/Login.tsx">
import { useState } from "react";
import { authActions } from "../lib/api";

export default function Login({
  setToken,
}: {
  setToken: (token: string) => void;
}) {
  const [isRegistering, setIsRegistering] = useState(false);
  const [email, setEmail] = useState("admin@test.com"); // Identity usually uses Email
  const [password, setPassword] = useState("Password123!"); // Identity requires strong pass

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    try {
      if (isRegistering) {
        await authActions.register({ email, password });
        alert("Registration successful! Please login.");
        setIsRegistering(false);
      } else {
        const data = await authActions.login({ email, password });
        // Log it to be 100% sure what the key is
        console.log("Identity Response:", data);

        const token = data.accessToken; // .NET 8 Identity uses 'accessToken'
        localStorage.setItem("token", token);
        setToken(token);
      }
    } catch (err) {
      alert(
        "Auth failed! Ensure you are using an Email and a Strong Password (length 8+, Uppercase, Number, Symbol).",
      );
    }
  };

  return (
    <div className="flex items-center justify-center min-h-screen bg-slate-50 p-4">
      <form
        onSubmit={handleSubmit}
        className="bg-white p-8 rounded-3xl shadow-xl w-full max-w-md border border-slate-100"
      >
        <h2 className="text-3xl font-black mb-2 text-center text-slate-800">
          {isRegistering ? "Create Account" : "Welcome Back"}
        </h2>
        <p className="text-slate-500 text-center mb-8">
          {isRegistering
            ? "Join GadgetStore today"
            : "Login to manage your gadgets"}
        </p>

        <div className="space-y-4">
          <input
            type="email"
            placeholder="Email Address"
            className="w-full p-4 bg-slate-50 border-none rounded-2xl focus:ring-2 focus:ring-blue-500"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
          />
          <input
            type="password"
            placeholder="Password"
            className="w-full p-4 bg-slate-50 border-none rounded-2xl focus:ring-2 focus:ring-blue-500"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
          />
          <button className="w-full bg-blue-600 text-white p-4 rounded-2xl font-bold text-lg hover:bg-blue-700 shadow-lg shadow-blue-200 transition">
            {isRegistering ? "Sign Up" : "Sign In"}
          </button>
        </div>

        <p className="mt-6 text-center text-slate-600 font-medium">
          {isRegistering
            ? "Already have an account?"
            : "Don't have an account?"}{" "}
          <button
            type="button"
            onClick={() => setIsRegistering(!isRegistering)}
            className="text-blue-600 font-bold hover:underline"
          >
            {isRegistering ? "Log In" : "Register Now"}
          </button>
        </p>
      </form>
    </div>
  );
}
</file>

<file path="src/components/LooksGallery.tsx">
import { ArrowUpRight } from "lucide-react";

export default function LooksGallery() {
  return (
    <section className="py-24 border-t border-slate-100 px-5 md:px-10">
      <div className="max-w-6xl mx-auto">
        {/* --- HEADER --- */}
        <div className="flex items-center justify-between mb-10 md:mb-12">
          <h2 className="text-[10px] md:text-[11px] font-black uppercase tracking-[0.4em] text-slate-400">
            Looks Gallery — Vol 01
          </h2>
          <button className="text-[10px] md:text-[11px] font-black uppercase tracking-widest text-slate-900 flex items-center gap-1.5 group">
            View Lookbook
            <ArrowUpRight
              size={14}
              className="group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-transform"
            />
          </button>
        </div>

        {/* --- MOBILE LAYOUT (Shown only on small screens) --- */}
        <div className="flex flex-col gap-4 md:hidden">
          {/* 1. Main Visual */}
          <div className="relative aspect-[4/5] overflow-hidden rounded-[2rem] bg-slate-100">
            <img
              src="https://images.unsplash.com/photo-1552374196-1ab2a1c593e8?w=800&q=80"
              className="w-full h-full object-cover object-top"
              alt="Editorial Main"
            />
          </div>

          {/* 2. Side-by-Side: Text + Detail 1 */}
          <div className="grid grid-cols-2 gap-4">
            <div className="flex flex-col justify-center p-4 rounded-[2rem] gap-y-3">
              <p className="text-[#b8f53e] font-black text-xl md:text-[9px] uppercase tracking-widest mb-3">
                The Balance
              </p>
              <h3 className="text-5xl md:text-xl font-black uppercase font-compressed leading-[0.9] tracking-tighter mb-3">
                Modern <br /> Elegance
              </h3>
              <p className="text-lg md:text-[11px] leading-relaxed text-slate-500 font-bold uppercase">
                Classic vibes meets modern trends.
              </p>
            </div>
            <div className="aspect-square overflow-hidden rounded-[2rem] bg-slate-100">
              <img
                src="https://images.unsplash.com/photo-1614251055880-ee96e4803393?w=600&q=80"
                className="w-full h-full object-cover object-top"
                alt="Detail 1"
              />
            </div>
          </div>

          {/* 3. Wide Detail Visual */}
          <div className="aspect-[16/9] overflow-hidden rounded-[2rem] bg-slate-100">
            <img
              src="https://images.unsplash.com/photo-1523275335684-37898b6baf30?w=600&q=80"
              className="w-full h-full object-cover"
              alt="Detail 2"
            />
          </div>
        </div>

        {/* --- DESKTOP LAYOUT (Shown only on md and up) --- */}
        <div className="hidden md:grid grid-cols-12 gap-10 items-center">
          {/* Group A: The Primary Image (Left) */}
          <div className="col-span-5">
            <div className="group relative aspect-[3/4] overflow-hidden rounded-[2.5rem] bg-slate-100 shadow-2xl shadow-slate-200">
              <img
                src="https://images.unsplash.com/photo-1552374196-1ab2a1c593e8?w=800&q=80"
                className="w-full h-full object-cover object-top transition-transform duration-[2.5s] ease-out group-hover:scale-105"
                alt="Editorial Main"
              />
              <div className="absolute inset-0 bg-black/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
            </div>
          </div>

          {/* Group B: Editorial Text (Center) */}
          <div className="col-span-3 flex flex-col justify-center z-10 px-4">
            <div className="space-y-6">
              <p className="text-[12px] font-black uppercase tracking-[0.2em] text-[#b8f53e] ">
                The Balance
              </p>
              <h3 className="text-2xl font-black uppercase font-compressed leading-none tracking-tighter text-slate-900">
                Elegance & <br /> Modernity
              </h3>
              <p className="text-[13px] leading-relaxed text-slate-500 font-bold uppercase tracking-tight">
                Our looks gallery showcases the perfect balance between classic
                elegance and modern trends.
              </p>
              <div className="w-10 h-1 bg-slate-900" />
              <p className="text-[12px] leading-relaxed text-slate-400 font-medium italic">
                "Craftsmanship meets confidence in every single stitch."
              </p>
            </div>
          </div>

          {/* Group C: Detail Stack (Right) */}
          <div className="col-span-4 grid grid-cols-1 gap-6">
            <div className="group relative aspect-square overflow-hidden rounded-[2rem] bg-slate-50 border border-slate-100">
              <img
                src="https://images.unsplash.com/photo-1614251055880-ee96e4803393?w=600&q=80"
                className="w-full h-full object-cover object-top transition-transform duration-[2s] group-hover:scale-110"
                alt="Detail 1"
              />
            </div>
            <div className="group relative aspect-[4/3] overflow-hidden rounded-[2rem] bg-slate-50 border border-slate-100">
              <img
                src="https://images.unsplash.com/photo-1523275335684-37898b6baf30?w=600&q=80"
                className="w-full h-full object-cover transition-transform duration-[2s] group-hover:scale-110"
                alt="Detail 2"
              />
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
</file>

<file path="src/components/Sidebar.tsx">
import { NavLink, useNavigate } from "react-router-dom";
import {
  LayoutGrid,
  ClipboardList,
  ShoppingCart,
  LogOut,
  Plus,
  Package,
} from "lucide-react";
import { useCartStore } from "../store/useCartStore";

const LIME = "#b8f53e";

const navItems = [
  { to: "/", label: "Marketplace", icon: LayoutGrid, end: true },
  { to: "/orders", label: "My Orders", icon: ClipboardList },
  { to: "/cart", label: "Checkout", icon: ShoppingCart },
];

export default function Sidebar() {
  const totalItems = useCartStore((s) => s.totalItems());
  const navigate = useNavigate();

  return (
    <aside
      style={{
        position: "fixed",
        left: 0,
        top: 0,
        height: "100vh",
        width: 240,
        background: "#fff",
        borderRight: "1px solid rgba(0,0,0,0.07)",
        display: "flex",
        flexDirection: "column",
        zIndex: 40,
        fontFamily: "'Helvetica Neue', Arial, sans-serif",
      }}
    >
      {/* Logo */}
      <div style={{ padding: "28px 24px 20px" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
          <div
            style={{
              width: 36,
              height: 36,
              background: "#2563eb",
              borderRadius: 10,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              flexShrink: 0,
            }}
          >
            <Package size={18} color="#fff" />
          </div>
          <span
            style={{
              fontWeight: 900,
              fontSize: 17,
              color: "#000",
              letterSpacing: "-0.02em",
            }}
          >
            NexStore
          </span>
        </div>
      </div>

      {/* Nav */}
      <nav
        style={{
          flex: 1,
          padding: "8px 12px",
          display: "flex",
          flexDirection: "column",
          gap: 2,
        }}
      >
        {navItems.map(({ to, label, icon: Icon, end }) => (
          <NavLink
            key={to}
            to={to}
            end={end}
            style={({ isActive }) => ({
              display: "flex",
              alignItems: "center",
              gap: 10,
              padding: "10px 14px",
              borderRadius: 12,
              textDecoration: "none",
              fontWeight: isActive ? 900 : 500,
              fontSize: 14,
              color: isActive ? "#fff" : "#555",
              background: isActive ? "#111" : "transparent",
              transition: "background 0.15s, color 0.15s",
              position: "relative",
            })}
            onMouseEnter={(e) => {
              const el = e.currentTarget as HTMLAnchorElement;
              if (!el.getAttribute("aria-current")) {
                el.style.background = "#f5f5f0";
                el.style.color = "#000";
              }
            }}
            onMouseLeave={(e) => {
              const el = e.currentTarget as HTMLAnchorElement;
              if (!el.getAttribute("aria-current")) {
                el.style.background = "transparent";
                el.style.color = "#555";
              }
            }}
          >
            {({ isActive }) => (
              <>
                <Icon size={16} color={isActive ? "#fff" : "currentColor"} />
                <span style={{ flex: 1 }}>{label}</span>
                {label === "Checkout" && totalItems > 0 && (
                  <span
                    style={{
                      background: LIME,
                      color: "#000",
                      fontSize: 10,
                      fontWeight: 900,
                      borderRadius: 20,
                      minWidth: 20,
                      height: 20,
                      display: "flex",
                      alignItems: "center",
                      justifyContent: "center",
                      padding: "0 5px",
                    }}
                  >
                    {totalItems}
                  </span>
                )}
              </>
            )}
          </NavLink>
        ))}
      </nav>

      {/* Bottom section */}
      <div
        style={{
          padding: "12px 12px 24px",
          display: "flex",
          flexDirection: "column",
          gap: 8,
        }}
      >
        {/* Add Product CTA */}
        <button
          onClick={() => navigate("/")}
          style={{
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            gap: 8,
            padding: "12px 16px",
            borderRadius: 12,
            background: "#111",
            color: "#fff",
            fontWeight: 900,
            fontSize: 13,
            border: "none",
            cursor: "pointer",
            width: "100%",
            transition: "background 0.15s, transform 0.1s",
          }}
          onMouseEnter={(e) =>
            ((e.currentTarget as HTMLButtonElement).style.background = "#333")
          }
          onMouseLeave={(e) =>
            ((e.currentTarget as HTMLButtonElement).style.background = "#111")
          }
          onMouseDown={(e) =>
            ((e.currentTarget as HTMLButtonElement).style.transform =
              "scale(0.97)")
          }
          onMouseUp={(e) =>
            ((e.currentTarget as HTMLButtonElement).style.transform =
              "scale(1)")
          }
        >
          <Plus size={15} />
          Browse Products
        </button>

        {/* Logout */}
        <button
          onClick={() => {
            localStorage.removeItem("accessToken");
            navigate("/login");
          }}
          style={{
            display: "flex",
            alignItems: "center",
            gap: 8,
            padding: "10px 14px",
            borderRadius: 12,
            background: "transparent",
            color: "#aaa",
            fontWeight: 600,
            fontSize: 13,
            border: "none",
            cursor: "pointer",
            width: "100%",
            transition: "color 0.15s, background 0.15s",
          }}
          onMouseEnter={(e) => {
            (e.currentTarget as HTMLButtonElement).style.color = "#e33";
            (e.currentTarget as HTMLButtonElement).style.background = "#fff5f5";
          }}
          onMouseLeave={(e) => {
            (e.currentTarget as HTMLButtonElement).style.color = "#aaa";
            (e.currentTarget as HTMLButtonElement).style.background =
              "transparent";
          }}
        >
          <LogOut size={15} />
          Logout
        </button>
      </div>
    </aside>
  );
}
</file>

<file path="src/lib/api.ts">
import axios from "axios";

const API_URL = "http://localhost:5050/api";

export const api = axios.create({
  baseURL: API_URL,
});

api.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem("token");
      // Only redirect if we aren't already on the login page
      if (!window.location.pathname.includes("login")) {
        window.location.href = "/";
      }
    }
    return Promise.reject(error);
  },
);

// --- TYPES ---
export interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  stockQuantity: number;
  category: string;
  imageUrl: string;
}

export interface Order {
  id: number;
  productId: number;
  quantity: number;
  totalAmount: number;
  status: string;
  createdAt: string;
}

// --- ACTIONS ---
export const authActions = {
  // .NET Identity API returns { accessToken, refreshToken, expiresIn, ... }
  login: (credentials: any) =>
    api.post("/auth/login", credentials).then((res) => res.data),
  register: (data: any) => api.post("/auth/register", data),
};

export const productActions = {
  getAll: () => api.get<Product[]>("/product").then((res) => res.data),
  getOne: (id: number) =>
    api.get<Product>(`/product/${id}`).then((res) => res.data),
  create: (data: any) => api.post("/product", data).then((res) => res.data),
};

export const orderActions = {
  placeOrder: (order: { productId: number; quantity: number }) =>
    api.post<Order>("/order", order).then((res) => res.data),
  getAll: () => api.get<Order[]>("/order").then((res) => res.data),
};

export const paymentActions = {
  createSession: (orderId: number) =>
    api
      .post<{ url: string }>("/payment/create-session", { OrderId: orderId })
      .then((res) => res.data),
};
</file>

<file path="src/lib/utils.ts">
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
</file>

<file path="src/pages/CartPage.tsx">
import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useMutation } from "@tanstack/react-query";
import { api } from "../lib/api";
import {
  Eye,
  EyeOff,
  ArrowRight,
  Loader2,
  Package,
  CheckCircle2,
} from "lucide-react";

type Mode = "login" | "register";

export default function LoginPage({
  setToken,
}: {
  setToken: (t: string) => void;
}) {
  const [mode, setMode] = useState<Mode>("login");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [showPass, setShowPass] = useState(false);
  const [error, setError] = useState("");
  const navigate = useNavigate();

  const loginMutation = useMutation({
    mutationFn: async () => {
      const { data } = await api.post("/auth/login", { email, password });
      return data;
    },
    onSuccess: (data) => {
      localStorage.setItem("token", data.accessToken);
      localStorage.setItem("userEmail", email); // For Sidebar profile
      setToken(data.accessToken);
      navigate("/");
    },
    onError: () => setError("Invalid credentials. Please try again."),
  });

  const registerMutation = useMutation({
    mutationFn: async () => {
      await api.post("/auth/register", { email, password });
    },
    onSuccess: () => {
      setMode("login");
      setError("");
      setPassword("");
      alert("Account created successfully. Please sign in.");
    },
    onError: () =>
      setError(
        "Registration failed. Use a strong password (8+ chars, uppercase, number).",
      ),
  });

  const isPending = loginMutation.isPending || registerMutation.isPending;

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setError("");
    if (mode === "login") loginMutation.mutate();
    else registerMutation.mutate();
  };

  return (
    <div className="flex min-h-screen bg-white font-sans selection:bg-[#b8f53e] selection:text-black">
      {/* ── LEFT PANEL: Editorial ── */}
      <div className="hidden lg:flex lg:w-1/2 bg-black p-16 flex-col justify-between relative overflow-hidden">
        {/* Glow Decor */}
        <div className="absolute top-1/4 -left-20 w-96 h-96 bg-[#b8f53e]/10 rounded-full blur-[120px] pointer-events-none" />

        <div className="relative z-10 flex items-center gap-3">
          <div className="bg-[#b8f53e] p-2 rounded-xl">
            <Package size={24} className="text-black" />
          </div>
          <span className="font-black text-2xl italic tracking-tighter text-white uppercase font-compressed">
            NexStore
          </span>
        </div>

        <div className="relative z-10">
          <p className="text-[#b8f53e] font-black text-[10px] uppercase tracking-[0.4em] mb-6">
            Premium Digital Commerce
          </p>
          <h1 className="text-white text-7xl xl:text-8xl font-black uppercase font-compressed leading-[0.8] tracking-tighter mb-8">
            Style <br /> Delivered <br />{" "}
            <span className="italic text-slate-500">Fast.</span>
          </h1>
          <p className="text-slate-400 font-bold uppercase text-[11px] tracking-widest leading-relaxed max-w-xs">
            A secure microservice-powered shopping experience built for the
            modern digital vanguard.
          </p>
        </div>

        <div className="relative z-10 space-y-4">
          {["Secure JWT Auth", "Instant Checkout", "Global Distribution"].map(
            (f) => (
              <div key={f} className="flex items-center gap-3">
                <CheckCircle2 size={16} className="text-[#b8f53e]" />
                <span className="text-slate-500 font-black uppercase text-[10px] tracking-widest">
                  {f}
                </span>
              </div>
            ),
          )}
        </div>
      </div>

      {/* ── RIGHT PANEL: Form ── */}
      <div className="flex-1 flex items-center justify-center p-8 md:p-16">
        <div className="w-full max-w-md">
          {/* Mobile Logo */}
          <div className="flex lg:hidden items-center gap-2 mb-12">
            <span className="font-black text-2xl italic tracking-tighter text-black uppercase font-compressed">
              NexStore
            </span>
          </div>

          <div className="inline-flex bg-slate-100 p-1.5 rounded-2xl mb-10 w-full">
            {(["login", "register"] as Mode[]).map((m) => (
              <button
                key={m}
                onClick={() => {
                  setMode(m);
                  setError("");
                }}
                className={`flex-1 py-3 rounded-xl font-black uppercase text-[10px] tracking-widest transition-all ${
                  mode === m
                    ? "bg-white text-black shadow-sm"
                    : "text-slate-400 hover:text-slate-600"
                }`}
              >
                {m === "login" ? "Sign In" : "Join Now"}
              </button>
            ))}
          </div>

          <header className="mb-10">
            <h2 className="text-4xl font-black uppercase font-compressed tracking-tighter text-slate-900 leading-none">
              {mode === "login" ? "Welcome Back" : "Create Account"}
            </h2>
            <p className="text-slate-400 font-bold uppercase text-[10px] tracking-widest mt-3">
              {mode === "login"
                ? "Enter your credentials to access the collection"
                : "Join the collective for exclusive drops"}
            </p>
          </header>

          <form onSubmit={handleSubmit} className="space-y-6">
            <div className="space-y-2">
              <label className="text-[10px] font-black uppercase tracking-widest text-slate-400 px-1">
                Email Address
              </label>
              <input
                type="email"
                required
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                className="w-full p-4 bg-slate-50 border-2 border-transparent rounded-2xl text-sm font-bold focus:bg-white focus:border-[#b8f53e]/30 outline-none transition-all"
                placeholder="YOU@EXAMPLE.COM"
              />
            </div>

            <div className="space-y-2">
              <label className="text-[10px] font-black uppercase tracking-widest text-slate-400 px-1">
                Password
              </label>
              <div className="relative">
                <input
                  type={showPass ? "text" : "password"}
                  required
                  value={password}
                  onChange={(e) => setPassword(e.target.value)}
                  className="w-full p-4 bg-slate-50 border-2 border-transparent rounded-2xl text-sm font-bold focus:bg-white focus:border-[#b8f53e]/30 outline-none transition-all"
                  placeholder="••••••••"
                />
                <button
                  type="button"
                  onClick={() => setShowPass(!showPass)}
                  className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-300 hover:text-black"
                >
                  {showPass ? <EyeOff size={18} /> : <Eye size={18} />}
                </button>
              </div>
            </div>

            {error && (
              <div className="p-4 bg-red-50 border border-red-100 rounded-2xl text-red-600 text-xs font-bold uppercase tracking-tight flex items-center gap-2">
                <AlertCircle size={14} /> {error}
              </div>
            )}

            <button
              type="submit"
              disabled={isPending}
              className="w-full py-5 bg-black text-white rounded-[1.5rem] font-black uppercase tracking-[0.2em] text-[11px] flex items-center justify-center gap-3 hover:bg-slate-800 transition-all active:scale-95 disabled:bg-slate-200"
            >
              {isPending ? (
                <Loader2 className="animate-spin" size={18} />
              ) : (
                <>
                  {mode === "login"
                    ? "Authorize Session"
                    : "Finalize Registration"}{" "}
                  <ArrowRight size={16} />
                </>
              )}
            </button>
          </form>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/pages/LoginPage.tsx">
import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useMutation } from "@tanstack/react-query";
import { api } from "../lib/api";
import { Eye, EyeOff, ArrowRight, Loader2, Package } from "lucide-react";
import { toast } from "sonner";

const LIME = "#b8f53e";

type Mode = "login" | "register";

export default function LoginPage({
  setToken,
}: {
  setToken: (token: string) => void;
}) {
  const [mode, setMode] = useState<Mode>("login");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [showPass, setShowPass] = useState(false);
  const [error, setError] = useState("");
  const navigate = useNavigate();

  const loginMutation = useMutation({
    mutationFn: async () => {
      const { data } = await api.post("/auth/login", { email, password });
      return data;
    },
    onSuccess: (data) => {
      localStorage.setItem("token", data.accessToken);
      localStorage.setItem("userEmail", email);
      setToken(data.accessToken); // Update parent state
      toast.success("Identity Verified", {
        description: `Welcome back, ${email.split("@")[0].toUpperCase()}`,
      });
      navigate("/");
    },

    onError: () => {
      toast.error("Authentication Failed", {
        description: "Invalid email or password. Please try again.",
      });
    },
  });

  const registerMutation = useMutation({
    mutationFn: async () => {
      await api.post("/auth/register", { email, password });
    },
    onSuccess: () => {
      setMode("login");
      setError("");
      setPassword("");
      toast.success("Account Created", {
        description: "You can now log in.",
      });
    },
    onError: () => {
      toast.error("Authentication Failed", {
        description: "Invalid email or password. Please try again.",
      });
    },
  });

  const isPending = loginMutation.isPending || registerMutation.isPending;

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setError("");
    if (mode === "login") loginMutation.mutate();
    else registerMutation.mutate();
  };

  const inputStyle = {
    width: "100%",
    padding: "12px 16px",
    background: "#f5f5f0",
    border: "1px solid transparent",
    borderRadius: 12,
    fontSize: 14,
    color: "#000",
    outline: "none",
    boxSizing: "border-box" as const,
    fontFamily: "inherit",
  };

  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#f4f4ef",
        display: "flex",
        fontFamily: "'Helvetica Neue', Arial, sans-serif",
      }}
    >
      {/* Left — editorial panel */}
      <div
        style={{
          width: "48%",
          background: "#111",
          display: "flex",
          flexDirection: "column",
          justifyContent: "space-between",
          padding: "52px 56px",
          position: "relative",
          overflow: "hidden",
        }}
        className="hidden lg:flex"
      >
        {/* Lime glow blob */}
        <div
          style={{
            position: "absolute",
            top: "30%",
            left: "-20%",
            width: 420,
            height: 420,
            background: `${LIME}22`,
            borderRadius: "50%",
            filter: "blur(80px)",
            pointerEvents: "none",
          }}
        />

        {/* Logo */}
        <div
          style={{
            display: "flex",
            alignItems: "center",
            gap: 10,
            position: "relative",
          }}
        >
          <div
            style={{
              width: 38,
              height: 38,
              background: LIME,
              borderRadius: 11,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
            }}
          >
            <Package size={18} color="#000" />
          </div>
          <span
            style={{
              fontWeight: 900,
              fontSize: 17,
              color: "#fff",
              letterSpacing: "-0.02em",
            }}
          >
            NexStore
          </span>
        </div>

        {/* Main copy */}
        <div style={{ position: "relative" }}>
          <p
            style={{
              fontSize: 11,
              fontWeight: 900,
              color: "#555",
              textTransform: "uppercase",
              letterSpacing: "0.15em",
              marginBottom: 16,
            }}
          >
            Premium Commerce
          </p>
          <h1
            style={{
              fontSize: "clamp(2.8rem, 4vw, 4rem)",
              fontWeight: 900,
              color: "#fff",
              lineHeight: 1.05,
              letterSpacing: "-0.04em",
              marginBottom: 20,
            }}
          >
            Style
            <br />
            Delivered
            <br />
            <span style={{ color: LIME }}>Fast.</span>
          </h1>
          <p
            style={{
              color: "#666",
              fontSize: 14,
              lineHeight: 1.7,
              maxWidth: 280,
            }}
          >
            A premium shopping experience built for the modern world. Explore
            thousands of products.
          </p>
        </div>

        {/* Feature list */}
        <div
          style={{
            display: "flex",
            flexDirection: "column",
            gap: 14,
            position: "relative",
          }}
        >
          {[
            "Instant checkout flow",
            "Order tracking dashboard",
            "Secure payment sessions",
          ].map((f, i) => (
            <div
              key={i}
              style={{ display: "flex", alignItems: "center", gap: 10 }}
            >
              <div
                style={{
                  width: 6,
                  height: 6,
                  borderRadius: "50%",
                  background: LIME,
                  flexShrink: 0,
                }}
              />
              <span style={{ fontSize: 13, color: "#888" }}>{f}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Right — form */}
      <div
        style={{
          flex: 1,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          padding: 32,
        }}
      >
        <div style={{ width: "100%", maxWidth: 400 }}>
          {/* Mobile logo */}
          <div
            className="flex lg:hidden"
            style={{ alignItems: "center", gap: 10, marginBottom: 40 }}
          >
            <div
              style={{
                width: 36,
                height: 36,
                background: LIME,
                borderRadius: 10,
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
              }}
            >
              <Package size={17} color="#000" />
            </div>
            <span style={{ fontWeight: 900, fontSize: 16, color: "#000" }}>
              NexStore
            </span>
          </div>

          {/* Mode tabs */}
          <div
            style={{
              display: "flex",
              background: "#ebebeb",
              borderRadius: 12,
              padding: 4,
              marginBottom: 32,
              gap: 4,
            }}
          >
            {(["login", "register"] as Mode[]).map((m) => (
              <button
                key={m}
                onClick={() => {
                  setMode(m);
                  setError("");
                }}
                style={{
                  flex: 1,
                  padding: "9px 0",
                  borderRadius: 9,
                  border: "none",
                  cursor: "pointer",
                  fontWeight: 900,
                  fontSize: 13,
                  background: mode === m ? "#fff" : "transparent",
                  color: mode === m ? "#000" : "#888",
                  boxShadow: mode === m ? "0 1px 4px rgba(0,0,0,0.08)" : "none",
                  transition: "all 0.15s",
                  fontFamily: "inherit",
                }}
              >
                {m === "login" ? "Sign In" : "Register"}
              </button>
            ))}
          </div>

          <h2
            style={{
              fontSize: 26,
              fontWeight: 900,
              color: "#000",
              letterSpacing: "-0.03em",
              marginBottom: 6,
            }}
          >
            {mode === "login" ? "Welcome back" : "Create account"}
          </h2>
          <p style={{ color: "#aaa", fontSize: 13, marginBottom: 28 }}>
            {mode === "login"
              ? "Sign in to your account to continue"
              : "Join NexStore today — it's free"}
          </p>

          <form
            onSubmit={handleSubmit}
            style={{ display: "flex", flexDirection: "column", gap: 16 }}
          >
            <div>
              <label
                style={{
                  display: "block",
                  fontSize: 12,
                  fontWeight: 700,
                  color: "#555",
                  marginBottom: 6,
                }}
              >
                Email address
              </label>
              <input
                type="email"
                required
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="you@example.com"
                style={inputStyle}
                onFocus={(e) =>
                  (e.currentTarget.style.border = `1px solid ${LIME}`)
                }
                onBlur={(e) =>
                  (e.currentTarget.style.border = "1px solid transparent")
                }
              />
            </div>

            <div>
              <label
                style={{
                  display: "block",
                  fontSize: 12,
                  fontWeight: 700,
                  color: "#555",
                  marginBottom: 6,
                }}
              >
                Password
              </label>
              <div style={{ position: "relative" }}>
                <input
                  type={showPass ? "text" : "password"}
                  required
                  value={password}
                  onChange={(e) => setPassword(e.target.value)}
                  placeholder="••••••••"
                  style={{ ...inputStyle, paddingRight: 44 }}
                  onFocus={(e) =>
                    (e.currentTarget.style.border = `1px solid ${LIME}`)
                  }
                  onBlur={(e) =>
                    (e.currentTarget.style.border = "1px solid transparent")
                  }
                />
                <button
                  type="button"
                  onClick={() => setShowPass(!showPass)}
                  style={{
                    position: "absolute",
                    right: 14,
                    top: "50%",
                    transform: "translateY(-50%)",
                    background: "none",
                    border: "none",
                    cursor: "pointer",
                    color: "#bbb",
                    padding: 0,
                    lineHeight: 0,
                  }}
                >
                  {showPass ? <EyeOff size={15} /> : <Eye size={15} />}
                </button>
              </div>
            </div>

            {error && (
              <div
                style={{
                  background: "#fef2f2",
                  border: "1px solid #fecaca",
                  color: "#dc2626",
                  fontSize: 13,
                  borderRadius: 10,
                  padding: "10px 14px",
                }}
              >
                {error}
              </div>
            )}

            <button
              type="submit"
              disabled={isPending}
              style={{
                padding: "14px 0",
                background: isPending ? "#ddd" : "#111",
                color: "#fff",
                fontWeight: 900,
                fontSize: 14,
                borderRadius: 12,
                border: "none",
                cursor: isPending ? "not-allowed" : "pointer",
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                gap: 8,
                transition: "background 0.15s, transform 0.1s",
                fontFamily: "inherit",
                marginTop: 4,
              }}
              onMouseDown={(e) => {
                if (!isPending)
                  (e.currentTarget as HTMLButtonElement).style.transform =
                    "scale(0.97)";
              }}
              onMouseUp={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.transform =
                  "scale(1)")
              }
              onMouseEnter={(e) => {
                if (!isPending)
                  (e.currentTarget as HTMLButtonElement).style.background =
                    "#333";
              }}
              onMouseLeave={(e) => {
                if (!isPending)
                  (e.currentTarget as HTMLButtonElement).style.background =
                    "#111";
              }}
            >
              {isPending ? (
                <Loader2 size={17} className="animate-spin" />
              ) : (
                <>
                  {mode === "login" ? "Sign In" : "Create Account"}{" "}
                  <ArrowRight size={15} />
                </>
              )}
            </button>
          </form>

          {/* Lime accent line under form */}
          <div
            style={{
              height: 2,
              background: LIME,
              borderRadius: 2,
              marginTop: 32,
              width: 40,
            }}
          />
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/pages/OrdersPage.tsx">
import { useQuery } from "@tanstack/react-query";
import { api } from "../lib/api";
import {
  Loader2,
  Clock,
  Package,
  CheckCircle2,
  AlertCircle,
} from "lucide-react";

interface Order {
  id: number;
  productId: number;
  quantity: number;
  totalAmount: number;
  status: string;
  createdAt: string;
}

const STATUS_MAP: Record<string, { label: string; class: string; icon: any }> =
  {
    Pending: {
      label: "Pending",
      class: "bg-amber-50 text-amber-600",
      icon: Clock,
    },
    Paid: {
      label: "Success",
      class: "bg-emerald-50 text-emerald-600",
      icon: CheckCircle2,
    },
    Failed: {
      label: "Declined",
      class: "bg-red-50 text-red-600",
      icon: AlertCircle,
    },
  };

export default function OrdersPage() {
  const {
    data: orders,
    isLoading,
    isError,
  } = useQuery<Order[]>({
    queryKey: ["orders"],
    queryFn: async () => {
      const { data } = await api.get("/order");
      return data;
    },
  });

  const formatDate = (iso: string) => {
    return new Date(iso)
      .toLocaleDateString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
      })
      .toUpperCase();
  };

  return (
    <div className="min-h-screen bg-white py-12">
      <div className="max-w-7xl mx-auto px-5 md:px-10">
        {/* Header Section */}
        <header className="mb-16">
          <div className="flex items-center gap-3 mb-4">
            <div className="bg-black p-1.5 rounded-lg">
              <Package size={16} className="text-[#b8f53e]" />
            </div>
            <p className="text-[10px] font-black text-slate-300 uppercase tracking-[0.4em]">
              Transaction History
            </p>
          </div>
          <h1 className="text-5xl md:text-7xl font-black uppercase font-compressed tracking-tighter text-slate-900 leading-none">
            Your Orders
          </h1>
        </header>

        {/* --- Content Area --- */}
        <div className="bg-white rounded-[2.5rem] border-2 border-slate-100 overflow-hidden shadow-2xl shadow-slate-100">
          {/* List Header */}
          <div className="hidden md:grid grid-cols-5 bg-slate-50/50 px-10 py-6 border-b-2 border-slate-100">
            {[
              "Reference",
              "Item Details",
              "Quantity",
              "Total Price",
              "Status",
            ].map((h) => (
              <span
                key={h}
                className="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]"
              >
                {h}
              </span>
            ))}
          </div>

          <div className="divide-y-2 divide-slate-100">
            {isLoading ? (
              <div className="py-32 flex justify-center">
                <Loader2 className="animate-spin text-slate-200" size={40} />
              </div>
            ) : isError ? (
              <div className="py-32 text-center text-red-500 font-bold uppercase text-xs tracking-widest">
                Failed to sync with OrderService
              </div>
            ) : orders?.length === 0 ? (
              <div className="py-32 text-center text-slate-300 font-bold uppercase text-xs tracking-[0.3em]">
                No transaction history found
              </div>
            ) : (
              orders?.map((order) => {
                const s = STATUS_MAP[order.status] || {
                  label: order.status,
                  class: "bg-slate-100",
                  icon: Clock,
                };
                return (
                  <div
                    key={order.id}
                    className="grid grid-cols-1 md:grid-cols-5 px-6 md:px-10 py-8 items-center gap-6 md:gap-0 hover:bg-slate-50 transition-colors"
                  >
                    <span className="font-black text-sm tracking-tight text-slate-900">
                      #ORD-{order.id.toString().padStart(4, "0")}
                    </span>

                    <div>
                      <span className="text-[11px] font-black uppercase tracking-widest text-slate-800">
                        SKU-{order.productId}
                      </span>
                      <p className="text-[10px] font-bold text-slate-400 mt-1 uppercase">
                        {formatDate(order.createdAt)}
                      </p>
                    </div>

                    <span className="text-sm font-bold text-slate-600">
                      {order.quantity} Units
                    </span>

                    <span className="text-lg font-black text-slate-900 font-compressed tracking-tight">
                      ₱{order.totalAmount.toLocaleString()}
                    </span>

                    <div
                      className={`flex items-center gap-2 px-4 py-2 rounded-full w-fit ${s.class}`}
                    >
                      <s.icon size={14} strokeWidth={3} />
                      <span className="text-[10px] font-black uppercase tracking-widest">
                        {s.label}
                      </span>
                    </div>
                  </div>
                );
              })
            )}
          </div>

          {/* Bottom Accent */}
          <div className="h-3 bg-[#b8f53e]" />
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/pages/ProductsPage.tsx">
import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { api } from "../lib/api";
import { useCartStore } from "../store/useCartStore";
import { ShoppingCart, Check, ArrowRight } from "lucide-react";
import StorefrontNav from "../components/storefront/StorefrontNav";
import Footer from "../components/Footer";
import LooksGallery from "@/components/LooksGallery";
import FeaturedScroll from "@/components/FeaturedScroll";

interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  imageUrl: string;
  category: string;
}

const HERO_PANELS = [
  {
    name: "Brown",
    tag: "New Drop",
    bg: "bg-[#e2ddd8]",
    imageUrl:
      "https://images.unsplash.com/photo-1552374196-1ab2a1c593e8?w=900&q=90",
    headline: "Redefine Your\nStyle with NexStore\nFashion…",
  },
  {
    name: "Arden Blake",
    tag: "SS 2025",
    bg: "bg-[#cfd3d8]",
    imageUrl:
      "https://images.unsplash.com/photo-1614251055880-ee96e4803393?w=900&q=90",
    headline: null,
  },
];

function HeroPanel({ panel, index }: { panel: any; index: number }) {
  return (
    <div
      className={`relative overflow-hidden group cursor-pointer w-full rounded-[2rem] h-[500px] md:h-full ${panel.bg}`}
    >
      <img
        src={panel.imageUrl}
        alt={panel.name}
        className="absolute inset-0 w-full h-full object-cover object-top transition-transform duration-[3s] ease-out group-hover:scale-105"
      />
      <div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent opacity-100 md:opacity-60" />
      <div className="absolute inset-0 p-8 md:p-12 flex flex-col justify-between z-10">
        <div className="flex justify-between items-start gap-4">
          {panel.headline ? (
            <h3 className="text-white text-xs md:text-lg lg:text-xl font-medium leading-tight uppercase max-w-[140px] md:max-w-[240px] drop-shadow-md">
              {panel.headline}
            </h3>
          ) : (
            <div />
          )}
          <span className="bg-[#b8f53e] text-black text-[9px] md:text-[10px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest whitespace-nowrap shadow-lg">
            {panel.tag}
          </span>
        </div>
        <div className="relative pb-2">
          {" "}
          {/* Added pb-2 for safe zone */}
          <h2 className="text-white text-5xl sm:text-6xl md:text-7xl lg:text-9xl font-black tracking-tighter uppercase font-compressed leading-[0.9] transition-all duration-700 ease-out group-hover:-translate-y-2">
            {panel.name}
          </h2>
        </div>
      </div>
    </div>
  );
}

export default function ProductsPage() {
  const [search, setSearch] = useState("");
  const [activeCategory, setActiveCategory] = useState("All");
  const [addedIds, setAddedIds] = useState<Set<number>>(new Set());
  const addItem = useCartStore((s) => s.addItem);

  const { data: products } = useQuery<Product[]>({
    queryKey: ["products"],
    queryFn: async () => {
      const { data } = await api.get("/product");
      return data;
    },
  });

  const apiCats = Array.from(new Set(products?.map((p) => p.category) ?? []));
  const categories = ["All", ...apiCats];

  const filtered = products?.filter((p) => {
    const q = search.toLowerCase();
    return (
      (p.name.toLowerCase().includes(q) ||
        p.description.toLowerCase().includes(q)) &&
      (activeCategory === "All" || p.category === activeCategory)
    );
  });

  const handleAdd = (product: Product) => {
    addItem({
      id: product.id,
      name: product.name,
      price: product.price,
      imageUrl: product.imageUrl,
    });
    setAddedIds((prev) => new Set(prev).add(product.id));
    setTimeout(
      () =>
        setAddedIds((prev) => {
          const n = new Set(prev);
          n.delete(product.id);
          return n;
        }),
      1600,
    );
  };

  return (
    <div className="min-h-screen bg-white text-black overflow-x-hidden">
      <StorefrontNav
        search={search}
        onSearchChange={setSearch}
        categories={categories}
        activeCategory={activeCategory}
        onCategoryChange={setActiveCategory}
      />

      {/* ── SECTION 1: CONSTRAINED (Hero + Grid) ── */}
      <div className="max-w-7xl mx-auto px-5 md:px-10">
        <section className="pt-4">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-3 h-auto md:h-[680px]">
            {HERO_PANELS.map((panel, i) => (
              <HeroPanel key={panel.name} panel={panel} index={i} />
            ))}
          </div>
        </section>

        <section className="mt-12 md:mt-24 mb-6 md:mb-10 flex items-end justify-between border-b border-slate-100 pb-4 md:border-none md:pb-0">
          <div className="flex-1">
            <p className="text-[9px] md:text-[10px] font-black text-slate-300 uppercase tracking-[0.3em] mb-1 md:mb-3">
              {activeCategory === "string"
                ? "COLLECTION 2025"
                : activeCategory.toUpperCase()}
            </p>
            <h2 className="text-3xl md:text-6xl font-black tracking-tighter uppercase italic font-compressed leading-[0.8] md:leading-none">
              Ready to Wear
            </h2>
          </div>
          <button className="flex items-center gap-2 text-[9px] md:text-[10px] font-black tracking-[0.2em] uppercase border-b-2 border-black pb-0.5 transition-all">
            Explore All <ArrowRight size={12} />
          </button>
        </section>

        <section className="mb-20">
          <div className="grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-x-3 gap-y-8 md:gap-x-8 md:gap-y-16">
            {filtered?.map((product) => (
              <ProductCard
                key={product.id}
                product={product}
                isAdded={addedIds.has(product.id)}
                onAdd={() => handleAdd(product)}
              />
            ))}
          </div>
        </section>
      </div>

      {/* ── SECTION 2: FULL BLEED (Black Background) ── */}
      {/* By moving this OUTSIDE the max-w-7xl div, it will touch both edges of the screen */}
      <FeaturedScroll />

      {/* ── SECTION 3: CONSTRAINED AGAIN (Gallery + Newsletter) ── */}
      <div className="max-w-7xl mx-auto px-5 md:px-10">
        <LooksGallery />

        <section className="bg-slate-50 py-24 rounded-[3rem] my-12 text-center px-6">
          <h2 className="text-5xl font-compressed font-black uppercase tracking-tighter mb-4">
            Join the Collective
          </h2>
          <p className="text-slate-500 font-bold uppercase text-[10px] tracking-[0.2em] mb-10">
            Get early access to limited drops and SS25 releases.
          </p>
          <div className="max-w-md mx-auto flex flex-col sm:flex-row gap-2">
            <input
              type="email"
              placeholder="YOUR@EMAIL.COM"
              className="flex-1 bg-white px-6 py-4 rounded-2xl text-[10px] font-bold outline-none ring-1 ring-slate-200 focus:ring-black transition-all"
            />
            <button className="bg-black text-white px-8 py-4 rounded-2xl font-black text-[10px] uppercase tracking-widest hover:bg-slate-800 transition-colors active:scale-95">
              Join
            </button>
          </div>
        </section>
      </div>

      <Footer />
    </div>
  );
}

// --- SUB COMPONENTS ---

function ValueProp({ title, desc }: { title: string; desc: string }) {
  return (
    <div>
      <h4 className="font-compressed font-black text-3xl uppercase tracking-tighter mb-1">
        {title}
      </h4>
      <p className="text-slate-400 text-[10px] font-bold uppercase tracking-widest leading-relaxed">
        {desc}
      </p>
    </div>
  );
}

function ProductCard({ product, isAdded, onAdd }: any) {
  return (
    <div className="group cursor-pointer flex flex-col w-full">
      {/* 
          Aspect Ratio: 3/4 is standard, but we ensure the image 
          doesn't have extra bottom margin 
      */}
      <div className="relative aspect-[3/4] rounded-[1.5rem] md:rounded-[2rem] overflow-hidden bg-slate-100">
        <img
          src={
            product.imageUrl ||
            "https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?w=500"
          }
          className="w-full h-full object-cover object-top transition-transform duration-700 group-hover:scale-110"
          alt={product.name}
        />

        <div className="absolute inset-x-0 bottom-0 p-3 md:p-4 translate-y-4 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-300 hidden md:block">
          <button
            onClick={(e) => {
              e.stopPropagation();
              onAdd();
            }}
            className={`w-full py-3 rounded-xl font-black text-[10px] uppercase tracking-widest flex items-center justify-center gap-2 shadow-xl ${
              isAdded ? "bg-[#b8f53e] text-black" : "bg-black text-white"
            }`}
          >
            {isAdded ? <Check size={14} /> : <ShoppingCart size={14} />}
            {isAdded ? "Added" : "Add to Bag"}
          </button>
        </div>
      </div>

      {/* --- Text Section: Tightened --- */}
      <div className="mt-3 md:mt-4 px-1">
        <div className="flex flex-col md:flex-row md:justify-between md:items-start gap-0.5 md:gap-4">
          <h3 className="text-[11px] md:text-sm font-black text-slate-800 tracking-tight leading-tight uppercase truncate">
            {product.name}
          </h3>
          <span className="text-[11px] md:text-sm font-black text-slate-900">
            ₱{product.price.toLocaleString()}
          </span>
        </div>
        <p className="text-[9px] md:text-[10px] font-bold text-slate-400 uppercase tracking-widest mt-0.5 md:mt-1">
          {product.category}
        </p>

        {/* On Mobile, show a simple '+' button for cart instead of the big hover button */}
        <button
          onClick={(e) => {
            e.stopPropagation();
            onAdd();
          }}
          className="md:hidden mt-3 w-full py-2 border border-slate-200 rounded-lg text-[10px] font-black uppercase tracking-tighter active:bg-slate-50 transition-colors"
        >
          {isAdded ? "✓ Added" : "+ Add to Bag"}
        </button>
      </div>
    </div>
  );
}
</file>

<file path="src/pages/SuccessPage.tsx">
import { useEffect } from "react";
import { useCartStore } from "../store/useCartStore";
import { useNavigate } from "react-router-dom";
import { CheckCircle2, ArrowRight, Package } from "lucide-react";

const LIME = "#b8f53e";

export default function SuccessPage() {
  const clearCart = useCartStore((s) => s.clearCart);
  const navigate = useNavigate();

  useEffect(() => {
    clearCart();
  }, [clearCart]);

  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#f4f4ef",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: 24,
        fontFamily: "'Helvetica Neue', Arial, sans-serif",
      }}
    >
      <div style={{ width: "100%", maxWidth: 440, textAlign: "center" }}>
        {/* Animated ring stack */}
        <div
          style={{
            position: "relative",
            width: 120,
            height: 120,
            margin: "0 auto 32px",
          }}
        >
          <span
            style={{
              position: "absolute",
              inset: 0,
              borderRadius: "50%",
              background: `${LIME}30`,
              animation: "ping 1.4s ease-out infinite",
            }}
          />
          <span
            style={{
              position: "absolute",
              inset: 8,
              borderRadius: "50%",
              background: `${LIME}20`,
              animation: "ping 1.4s ease-out infinite",
              animationDelay: "0.3s",
            }}
          />
          <div
            style={{
              position: "absolute",
              inset: 0,
              borderRadius: "50%",
              background: LIME,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              boxShadow: `0 16px 40px ${LIME}80`,
            }}
          >
            <CheckCircle2 size={52} color="#000" strokeWidth={2.5} />
          </div>
        </div>

        <style>{`
          @keyframes ping {
            0% { transform: scale(0.95); opacity: 0.8; }
            70% { transform: scale(1.3); opacity: 0; }
            100% { transform: scale(1.3); opacity: 0; }
          }
        `}</style>

        {/* Card */}
        <div
          style={{
            background: "#fff",
            borderRadius: 24,
            padding: "36px 32px",
            border: "1px solid rgba(0,0,0,0.06)",
            overflow: "hidden",
            position: "relative",
          }}
        >
          <h1
            style={{
              fontSize: 28,
              fontWeight: 900,
              color: "#000",
              letterSpacing: "-0.03em",
              marginBottom: 8,
            }}
          >
            Payment Successful!
          </h1>
          <p
            style={{
              color: "#aaa",
              fontSize: 14,
              lineHeight: 1.6,
              marginBottom: 32,
            }}
          >
            Your order has been placed and is being processed. You'll receive a
            confirmation shortly.
          </p>

          {/* Divider */}
          <div
            style={{
              display: "flex",
              alignItems: "center",
              gap: 12,
              marginBottom: 28,
            }}
          >
            <div style={{ flex: 1, height: 1, background: "#f0f0ec" }} />
            <div
              style={{
                width: 32,
                height: 32,
                background: LIME,
                borderRadius: 10,
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
              }}
            >
              <Package size={14} color="#000" />
            </div>
            <div style={{ flex: 1, height: 1, background: "#f0f0ec" }} />
          </div>

          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            <button
              onClick={() => navigate("/orders")}
              style={{
                width: "100%",
                padding: "14px 0",
                background: "#111",
                color: "#fff",
                fontWeight: 900,
                fontSize: 14,
                borderRadius: 14,
                border: "none",
                cursor: "pointer",
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                gap: 8,
                transition: "background 0.15s, transform 0.1s",
              }}
              onMouseDown={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.transform =
                  "scale(0.97)")
              }
              onMouseUp={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.transform =
                  "scale(1)")
              }
              onMouseEnter={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.background =
                  "#333")
              }
              onMouseLeave={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.background =
                  "#111")
              }
            >
              View Your Orders <ArrowRight size={15} />
            </button>
            <button
              onClick={() => navigate("/")}
              style={{
                width: "100%",
                padding: "14px 0",
                background: "#f5f5f0",
                color: "#555",
                fontWeight: 900,
                fontSize: 14,
                borderRadius: 14,
                border: "none",
                cursor: "pointer",
                transition: "background 0.15s, transform 0.1s",
              }}
              onMouseDown={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.transform =
                  "scale(0.97)")
              }
              onMouseUp={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.transform =
                  "scale(1)")
              }
              onMouseEnter={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.background =
                  "#ebebeb")
              }
              onMouseLeave={(e) =>
                ((e.currentTarget as HTMLButtonElement).style.background =
                  "#f5f5f0")
              }
            >
              Continue Shopping
            </button>
          </div>

          {/* Lime bar at bottom */}
          <div
            style={{
              position: "absolute",
              bottom: 0,
              left: 0,
              right: 0,
              height: 4,
              background: LIME,
            }}
          />
        </div>

        <p style={{ color: "#bbb", fontSize: 11, marginTop: 20 }}>
          Order confirmed · Powered by NexStore
        </p>
      </div>
    </div>
  );
}
</file>

<file path="src/store/useCartStore.ts">
import { create } from "zustand";
import { persist } from "zustand/middleware";

export interface CartItem {
  id: number;
  name: string;
  price: number;
  imageUrl: string;
  quantity: number;
}

interface CartStore {
  items: CartItem[];
  addItem: (product: Omit<CartItem, "quantity">) => void;
  removeItem: (id: number) => void;
  updateQuantity: (id: number, quantity: number) => void;
  clearCart: () => void;
  totalItems: () => number;
  totalPrice: () => number;
}

export const useCartStore = create<CartStore>()(
  persist(
    (set, get) => ({
      items: [],
      addItem: (product) => {
        const existing = get().items.find((i) => i.id === product.id);
        if (existing) {
          set((s) => ({
            items: s.items.map((i) =>
              i.id === product.id ? { ...i, quantity: i.quantity + 1 } : i,
            ),
          }));
        } else {
          set((s) => ({ items: [...s.items, { ...product, quantity: 1 }] }));
        }
      },
      removeItem: (id) =>
        set((s) => ({ items: s.items.filter((i) => i.id !== id) })),
      updateQuantity: (id, quantity) => {
        if (quantity <= 0) {
          get().removeItem(id);
          return;
        }
        set((s) => ({
          items: s.items.map((i) => (i.id === id ? { ...i, quantity } : i)),
        }));
      },
      clearCart: () => set({ items: [] }),
      totalItems: () => get().items.reduce((sum, i) => sum + i.quantity, 0),
      totalPrice: () =>
        get().items.reduce((sum, i) => sum + i.price * i.quantity, 0),
    }),
    { name: "cart-store" },
  ),
);
</file>

<file path="src/App.tsx">
import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState, useEffect } from "react";
import { Toaster } from "sonner"; // 1. Import Sonner
import StorefrontNav from "./components/storefront/StorefrontNav";
import Marketplace from "./pages/ProductsPage";
import CartPage from "./pages/CartPage";
import OrdersPage from "./pages/OrdersPage";
import SuccessPage from "./pages/SuccessPage";
import LoginPage from "./pages/LoginPage";
import { ProtectedRoute, PublicRoute } from "./components/AuthGuard";
import Footer from "./components/Footer";

const queryClient = new QueryClient();

export default function App() {
  const [token, setToken] = useState(localStorage.getItem("token"));

  // This effect ensures the app re-renders when localStorage changes
  useEffect(() => {
    const handleStorage = () => setToken(localStorage.getItem("token"));
    window.addEventListener("storage", handleStorage);
    return () => window.removeEventListener("storage", handleStorage);
  }, []);

  const handleLogout = () => {
    localStorage.removeItem("token");
    localStorage.removeItem("userEmail");
    setToken(null);
    window.location.href = "/"; // Hard redirect to clear state
  };

  return (
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        {/* 2. Add Toaster at the root */}
        <Toaster position="top-center" expand={false} richColors closeButton />

        <div className="min-h-screen bg-white flex flex-col selection:bg-[#b8f53e] selection:text-black">
          <main className="flex-1">
            <Routes>
              {/* ── PUBLIC ROUTE (Landing Page) ── */}
              <Route path="/" element={<Marketplace />} />

              {/* ── GUEST ONLY (Redirects to home if already logged in) ── */}
              <Route element={<PublicRoute />}>
                <Route
                  path="/login"
                  element={<LoginPage setToken={setToken} />}
                />
              </Route>

              {/* ── PROTECTED ROUTES (Requires Login) ── */}
              <Route element={<ProtectedRoute />}>
                <Route path="/cart" element={<CartPage />} />
                <Route path="/orders" element={<OrdersPage />} />
                <Route path="/success" element={<SuccessPage />} />
              </Route>

              {/* Catch-all */}
              <Route path="*" element={<Navigate to="/" />} />
            </Routes>
          </main>

          <Footer />
        </div>
      </BrowserRouter>
    </QueryClientProvider>
  );
}
</file>

<file path="src/index.css">
@import url("https://fonts.googleapis.com/css2?family=Inter+Tight:wght@100;400;900&display=swap");

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-white text-slate-900 antialiased;
  }
}

@layer utilities {
  .font-compressed {
    font-stretch: 75%;
    letter-spacing: -0.04em;
    font-variation-settings: "wdth" 75;
  }
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
</file>

<file path="src/main.tsx">
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
</file>

<file path=".gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
</file>

<file path="components.json">
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "src/index.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "iconLibrary": "lucide",
  "rtl": false,
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "registries": {}
}
</file>

<file path="eslint.config.js">
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
  },
])
</file>

<file path="index.html">
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>frontend</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
</file>

<file path="package.json">
{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-dropdown-menu": "^2.1.16",
    "@radix-ui/react-separator": "^1.1.8",
    "@tanstack/react-query": "^5.90.21",
    "axios": "^1.13.5",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.575.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.13.1",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.5.0",
    "tailwindcss-animate": "^1.0.7",
    "zustand": "^5.0.11"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@types/node": "^22.19.12",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.27",
    "eslint": "^9.14.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.14",
    "globals": "^15.12.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "typescript": "~5.6.3",
    "typescript-eslint": "^8.13.0",
    "vite": "^5.4.11"
  }
}
</file>

<file path="postcss.config.js">
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
</file>

<file path="README.md">
# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend updating the configuration to enable type-aware lint rules:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
</file>

<file path="tailwind.config.js">
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        // Adding your Inter Tight font here
        sans: ['"Inter Tight"', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
</file>

<file path="tsconfig.app.json">
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "types": ["vite/client"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,

    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  },
  "include": ["src"]
}
</file>

<file path="tsconfig.json">
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
    "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
</file>

<file path="tsconfig.node.json">
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "ESNext",
    "types": ["node"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["vite.config.ts"]
}
</file>

<file path="vite.config.ts">
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "path";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"), // 2. Map @ to the src folder
    },
  },
});
</file>

</files>
